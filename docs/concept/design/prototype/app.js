/* UI/UX review fixture only. No filesystem, subprocess, network, or Obsidian API access. */
'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths={box:'M12 3 3 8v9l9 5 9-5V8L12 3Zm-9 5 9 5 9-5M12 13v9',search:'m20 20-5-5M17 10a7 7 0 1 1-14 0 7 7 0 0 1 14 0',folder:'M3 6h7l2 2h9v12H3V6Z',file:'M6 3h8l4 4v14H6V3Zm8 0v5h4M9 13h6M9 17h6',settings:'M9 3h6l1 4 4 2v6l-4 2-1 4H9l-1-4-4-2V9l4-2 1-4Zm6 9a3 3 0 1 1-6 0 3 3 0 0 1 6 0',chevron:'m9 6 6 6-6 6',down:'m6 9 6 6 6-6',close:'m6 6 12 12M6 18 18 6',refresh:'M20 7v5h-5M4 17v-5h5M5 8a7 7 0 0 1 12-3l3 7M4 12l3 7a7 7 0 0 0 12-3',fit:'M8 3H3v5M16 3h5v5M3 16v5h5M21 16v5h-5',top:'M4 4h16v16H4V4ZM4 12h16M12 4v16',plus:'M12 5v14M5 12h14',minus:'M5 12h14',help:'M12 16h.01M9 8a3 3 0 1 1 5 2c-2 1-2 2-2 3M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',check:'m5 12 4 4L19 6',alert:'m12 3 10 18H2L12 3Zm0 5v6m0 3h.01',external:'M14 3h7v7M21 3 10 14M10 3H3v18h18v-7',copy:'M8 8h13v13H8V8ZM4 16H2V2h14v2',note:'M4 3h16v18H4V3Zm4 5h8M8 12h8M8 16h5',link:'m9 15 6-6M8 17l-2 2a4 4 0 0 1-6-6l4-4a4 4 0 0 1 6 0M14 7l2-2a4 4 0 0 1 6 6l-4 4a4 4 0 0 1-6 0',graph:'M7 7 17 17M7 17 17 7M8 5a3 3 0 1 1-6 0 3 3 0 0 1 6 0M22 5a3 3 0 1 1-6 0 3 3 0 0 1 6 0M8 19a3 3 0 1 1-6 0 3 3 0 0 1 6 0M22 19a3 3 0 1 1-6 0 3 3 0 0 1 6 0',list:'M8 5h13M8 12h13M8 19h13M3 5h.01M3 12h.01M3 19h.01',shield:'m12 2 9 4v6c0 5-9 10-9 10S3 17 3 12V6l9-4Zm-4 10 3 3 5-6',clock:'M12 7v6l4 2M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',branch:'M6 3v18M18 3v4c0 5-12 3-12 9M9 5a3 3 0 1 1-6 0 3 3 0 0 1 6 0M21 5a3 3 0 1 1-6 0 3 3 0 0 1 6 0',arrow:'M4 12h16m-6-6 6 6-6 6',dots:'M5 12h.01M12 12h.01M19 12h.01',play:'m7 4 14 8-14 8V4Z'};
const icon=(name)=>`<svg class="svg-icon" viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[name]||paths.box}"/></svg>`;
const btn=(text,action,cls='',ico='')=>`<button class="${cls}" data-action="${action}">${ico?icon(ico)+' ':''}${text}</button>`;
const ib=(name,label,action)=>`<button class="icon-btn" data-action="${action}" aria-label="${label}" title="${label}">${icon(name)}</button>`;
let q=new URLSearchParams(location.search), screen=DESIGN_SCREENS.find(x=>x.id===(q.get('screen')||'S05'))||DESIGN_SCREENS[4];
let selected=DESIGN_DATA.selectedId, query='',topMode=false,zoom=1,pan={x:0,y:0},drawer=true,sourceKind='external',help=false,noteCreated=false,scopeAcknowledged=false,inspectorClosed=false,selectionActive=true;
const changedCity=()=>CITY_ISO.replace(/class="building" data-file="(f072|f024|f089)"/g,'class="building changed" data-file="$1"');
const selectedFile=()=>DESIGN_DATA.files.find(f=>f.id===selected)||DESIGN_DATA.files.find(f=>f.id===DESIGN_DATA.selectedId);
let timer=null;
if(q.has('capture'))document.body.classList.add('capture');
function footer(){return `<div class="view-footer"><span>144 included files · 6 districts</span><span>Inventory · 17 Sep 2026, 13:00</span><span class="right">Read-only source access</span></div>`}
function host(content){return `<div class="host-title">${icon('folder')}<span>Development vault</span><span class="global-title-caption muted">/ ${esc(screen.title)}</span><div class="window-controls"><span>─</span><span>□</span><span>×</span></div></div><div class="host-body"><aside class="ribbon" aria-label="Illustrative Obsidian ribbon">${ib('folder','Host files','noop')}${ib('search','Host search','noop')}${ib('graph','Host graph','noop')}<div class="active">${ib('box','Open Codebase Inspector','city')}</div><div class="grow"></div>${ib('settings','Codebase Inspector settings','settings')}</aside><aside class="host-nav"><div class="nav-title">Files ${icon('dots')}</div><div class="folder">⌄ Engineering</div><div class="nav-file">Architecture overview</div><div class="nav-file">Quality strategy</div><div class="folder">⌄ Investigations</div><div class="nav-file active">Layout complexity</div><div class="nav-file">Import boundaries</div><div class="folder">› Decisions</div><div class="folder">› Meetings</div></aside><main class="workspace"><div class="tabsbar"><div class="workspace-tab">${icon(screen.slug==='embed'?'note':'box')}<span>${screen.slug==='embed'?'Layout complexity':'Codebase Inspector'}</span><span class="tab-x">×</span></div></div><div class="view" id="plugin-view">${content}</div></main></div><div class="host-status"><span>${screen.id} · ${screen.title} · WP-${String(screen.package).padStart(2,'0')}</span><span>DESIGN MOCKUP · synthetic data · not a repository analysis</span></div>`}
function toolbar(options={}){return `<div class="toolbar">${btn('<span><strong>Codebase Inspector</strong><small>External directory · demo profile</small></span>'+icon('down'),'source','profile-button','folder')}<div class="search">${icon('search')}<input id="file-search" aria-label="Search codebase files" placeholder="Search files or paths…" value="${esc(query)}"><kbd>/</kbd></div>${options.lens?`<select id="lens-select" class="compact-select" aria-label="Color buildings by"><option value="category">File category</option><option value="findings" ${['findings','failure'].includes(screen.slug)?'selected':''}>Reported findings</option>${screen.package>=6?'<option value="coverage" '+(screen.slug==='coverage'?'selected':'')+'>Measured coverage</option>':''}${screen.package>=12?'<option value="runtime" '+(screen.slug==='runtime'?'selected':'')+'>Observed execution</option>':''}</select>`:''}<div class="end">${btn('Scan','scan','','refresh')}${ib('settings','Open plugin settings','settings')}</div></div>`}
function modes(active='city'){return `<nav class="local-modes" aria-label="Inspector workspace">${[['City','city',1],['Findings','workbench',4],['Compare','compare',5],['Overview','overview',9]].filter(x=>screen.package>=x[2]).map(([t,a])=>btn(t,a,active===a?'active':'')).join('')}</nav>`}
function listPanel(){let fs=query?DESIGN_DATA.files.filter(f=>f.path.toLowerCase().includes(query.toLowerCase())):DESIGN_DATA.files.filter(f=>f.group==='src/visualization');return `<aside class="file-panel" aria-label="Codebase file list"><div class="panel-heading">${query?'SEARCH RESULTS':'CODEBASE FILES'} ${icon('list')}</div>${query?`<div class="tree-note">${fs.length} matches · 144 total files</div>`:'<div class="group-label">› src/domain <span class="grow"></span>24</div><div class="group-label">› src/application <span class="grow"></span>24</div><div class="group-label">› src/adapters <span class="grow"></span>24</div><div class="group-label">⌄ src/visualization <span class="grow"></span>24</div>'}<ul class="file-list">${fs.slice(0,18).map(f=>`<li><button class="file-row ${selected===f.id&&!inspectorClosed?'selected':''}" data-file-id="${f.id}"><span class="file-type">${f.category==='Vue'?'V':'TS'}</span><span class="filename">${esc(f.name)}</span>${screen.package>1&&f.findings?'<span class="dot"></span>':''}</button></li>`).join('')}</ul>${!query?'<div class="group-label">› src/ui <span class="grow"></span>24</div><div class="group-label">› tests <span class="grow"></span>24</div>':''}<div class="tree-note">${query?'Search changes emphasis, not building positions.':'Select a file to inspect it. This list is independent of the vault’s note explorer.'}</div></aside>`}
function inspector(extra=''){let f=selectedFile();if(inspectorClosed||screen.slug==='city')return '';let quality=['findings','failure','workbench'].includes(screen.slug), cov=screen.slug==='coverage',run=screen.slug==='runtime';return `<aside class="inspector" aria-label="Selected file inspector"><div class="drawer-head">File inspector ${ib('close','Close inspector','close-inspector')}</div><div class="title-row">${icon('file')}<span class="pill">${f.category}</span><span class="grow"></span>${ib('close','Close file selection','clear-selection')}</div><h2 class="selected-name">${esc(f.name)}</h2><div class="path mono">${esc(f.path)}</div><div class="row"><span class="pill accent">Selected file</span>${quality?'<span class="pill warn">2 findings</span>':''}</div><div class="section-label">${cov?'Test evidence':run?'Execution evidence':'File measurements'}</div>${cov?`<div class="metric-row"><span>Line coverage</span><strong>62 / 100</strong></div><div class="inline-progress"><span style="width:62%"></span></div><div class="metric-row"><span>Branch coverage</span><strong>10 / 20</strong></div><div class="metric-row"><span>Measured at</span><strong>13:00</strong></div><p class="small mt">Source-mapped test report. Physical source lines and instrumented lines are different totals.</p>`:run?`<div class="metric-row"><span>Observation</span><strong>Observed</strong></div><div class="metric-row"><span>Environment</span><strong>Staging</strong></div><div class="metric-row"><span>Build</span><strong class="mono">demo-9d41</strong></div><p class="small mt">Sampled capture. This is not a production-wide usage claim.</p>`:`<div class="metric-row"><span>Physical lines</span><strong>${f.lines.toLocaleString()}</strong></div><div class="metric-row"><span>File size</span><strong>${(f.bytes/1024).toFixed(1)} KiB</strong></div><div class="metric-row"><span>Directory</span><strong>${f.group.split('/').pop()}</strong></div><p class="small mt">Physical lines include comments and blank lines. Height does not represent complexity.</p>`}${quality?`<div class="section-label">Reported evidence</div><div class="evidence-box"><strong>Cognitive complexity: 27</strong><p>partitionDistrict · line 84<br>Reported above threshold 15</p></div><div class="note neutral">Other functions are not necessarily measured. No finding is not the same as zero complexity.</div><div class="small mt">Provider: fallow · imported report<br>Snapshot: demo-2026-09-17</div>`:''}${screen.slug==='dependencies'?`<div class="section-label">Imports from this file</div><div class="metric-row"><span>Shown edges</span><strong>6</strong></div><p class="small mt">Arrow: importer → imported module. Import graph, not execution order.</p><div class="evidence-box mono">city-layout.ts → bounds.ts<br>city-layout.ts → layout-types.ts</div>`:''}${extra}<div class="action-stack">${btn('Focus in city','focus','','fit')}${btn('Copy relative path','copy-path','','copy')}${screen.package>=4?btn('Create investigation note','note','primary','note'):''}</div><div class="rule"></div><div class="small">Source remains unchanged.<br>Source-opening actions are explicit.</div></aside>`}
function scene(){let s=screen.slug,quality=['findings','failure'].includes(s);let cls=s==='dependencies'?'dependencies':quality?'quality':s==='coverage'?'coverage':s==='runtime'?'runtime':'';let title=s==='dependencies'?'One-hop neighborhood':quality?'Reported findings':s==='coverage'?'Measured test coverage':s==='runtime'?'Observed execution':topMode?'Top-down city':'Codebase city';let sub=s==='dependencies'?'6 directed edges · outgoing imports only':quality?'12 findings · 11 files · imported evidence':s==='coverage'?'108 files measured · 36 not instrumented':s==='runtime'?'Staging · 10–17 Sep 2026 · build demo-9d41':'144 files grouped into 6 directory districts';let legend=s==='coverage'?'<span><i class="swatch" style="background:#77a4cc"></i>Measured</span><span><i class="swatch" style="background:#c88b94"></i>Measured zero</span><span>▨ Not instrumented</span>':quality?'<span><i class="swatch" style="background:#dbad78"></i>Reported finding</span><span>▨ No finding reported / metric unavailable</span>':s==='runtime'?'<span><i class="swatch" style="background:#77a4cc"></i>Observed</span><span><i class="swatch" style="background:#d9b57a"></i>Not observed</span><span>▨ Not instrumented</span>':'<span><i class="swatch" style="background:#77a4cc"></i>TypeScript</span><span><i class="swatch" style="background:#76b6a4"></i>Vue</span><span><i class="swatch" style="background:#b29bcd"></i>Tests</span>';
return `<section class="scene-wrap ${cls}" aria-label="Codebase city"><div class="scene-head"><div><div class="eyebrow">${s==='dependencies'?'Architecture lens':quality?'Fallow lens':s==='coverage'?'Coverage lens':s==='runtime'?'Runtime lens':'Structure · physical inventory'}</div><h2>${title}</h2><p>${sub}</p></div><span class="pill">${quality?'Report imported':'Read-only snapshot'}</span></div><div class="scene-host" id="scene-host" tabindex="0" aria-label="City illustration. Use file list, search, and camera buttons for equivalent access.">${topMode?CITY_TOP:CITY_ISO}</div><div class="narrow-bottom">${btn('Files','fallback','','list')}</div><div class="scene-controls">${ib('minus','Zoom out','zoom-out')}${ib('plus','Zoom in','zoom-in')}${btn('Fit','fit','','fit')}${btn(topMode?'3D':'Top','top','','top')}${ib('help','Camera interaction help','help')}</div>${help?`<div class="camera-help"><strong>City controls</strong><br>Click: select · Double-click: focus<br>Production: left drag orbits; right drag pans.<br>This prototype: drag pans an SVG illustration.<br>Use Fit, Top, and zoom buttons without dragging.<br><kbd>Esc</kbd> closes this help first.</div>`:''}<div class="scene-legend"><div class="legend-card">${legend}</div><div class="legend-line"><span>Height: physical lines · square-root scale</span><span>One equal lot per file</span><span>Selected file has an outline + marker</span></div></div></section>`}
function cityContent(){let b='';if(screen.slug==='failure')b=`<div class="banner">${icon('alert')}<div><strong>Fallow analysis failed.</strong> The structural snapshot is still available.<br><span class="small">Showing imported evidence from 16 Sep 2026. It is not current.</span></div>${btn('Review provider','provider')}</div>`;if(screen.slug==='cancelled')b=`<div class="banner">${icon('alert')}<div><strong>Scan cancelled.</strong> The incomplete result was discarded.<br><span class="small">Your complete snapshot from 13:00 is unchanged.</span></div>${btn('Scan again','scan')}</div>`;return toolbar({lens:screen.package>1})+(screen.package>1?modes(): '')+b+(screen.slug==='search'?`<div class="filter-strip">${icon('search')} ${DESIGN_DATA.files.filter(f=>f.path.includes(query)).length} matching paths <span class="pill">${esc(query)}</span>${btn('Clear search','clear-search','subtle')}</div>`:'')+(screen.slug==='dependencies'?`<div class="filter-strip"><span>Direction: outgoing</span><span class="pill">1 hop</span><span>6 shown · 0 hidden</span><span class="grow"></span>${btn('Show edge list','edge-list','subtle')}</div>`:'')+`<div class="workspace-content">${listPanel()}${scene()}${inspector()}</div>`+footer()}
function welcome(){return `<div class="center-stage"><div class="welcome-card"><div class="overline">Codebase Inspector</div><h1>Understand your codebase.<br>Start with its structure.</h1><p class="intro">Choose a local codebase to explore its files and directories as a city. Analysis reports can be added later.</p><button class="option-card" data-action="source"> <span class="option-icon">${icon('folder')}</span><span><strong>Select a codebase</strong><small>Current vault, vault directory, or external directory</small></span><span class="grow"></span>${icon('arrow')}</button><div class="rule"></div><div class="row">${icon('shield')}<span>Nothing is scanned when the plugin is enabled.</span></div><p class="small mt">Inventory reads the selected files. It does not install dependencies, execute project scripts, or change source code.</p></div></div>`}
function sourceModal(){return `<div class="modal-backdrop"><section class="modal" role="dialog" aria-modal="true" aria-labelledby="dialog-title"><div class="modal-header"><h2 id="dialog-title">Select a codebase</h2>${ib('close','Close source selection','city')}</div><p>Choose where the source lives. Your notes can stay in this vault.</p>${[['vault','Current vault','Inspect this vault’s underlying directory.'],['directory','Directory in this vault','Limit the inventory to one project folder.'],['external','External directory','Read a local codebase outside this vault.']].map(([id,t,sub])=>`<button class="option-card ${sourceKind===id?'selected':''}" data-source="${id}"><span class="option-icon">${icon('folder')}</span><span><strong>${t}</strong><small>${sub}</small></span><span class="radio"></span></button>`).join('')}<div class="form-field"><label for="source-path">${sourceKind==='external'?'Absolute directory path':sourceKind==='directory'?'Vault-relative directory':'Resolved vault directory'}</label><input id="source-path" value="${sourceKind==='external'?'C:\\dev\\codebase-inspector':sourceKind==='directory'?'Projects/codebase-inspector':'C:\\notes\\development'}"><span class="help">${sourceKind==='external'?'External paths are stored as machine-local bindings.':'No source files are copied or moved.'}</span></div><div class="modal-footer">${btn('Cancel','city')}${btn('Review scope','scope','primary')}</div></section></div>`}
function scopeModal(){return `<div class="modal-backdrop"><section class="modal" role="dialog" aria-modal="true" aria-labelledby="dialog-title"><div class="modal-header"><h2 id="dialog-title">Review scope and read access</h2>${ib('close','Close scope review','city')}</div><p>Confirm the directory and exclusions before starting an inventory.</p><div class="note neutral source-summary"><strong>Codebase Inspector</strong><br><span class="mono">C:\\dev\\codebase-inspector</span><br><span class="small">External directory · resolved path</span></div><div class="section-label">Excluded by default</div><div class="scope-list">${['.git','node_modules','dist','build','coverage','secrets','plugin output'].map(t=>`<span class="pill mono">${t}</span>`).join('')}</div><p class="small mt">Symlinks and junctions are skipped. Unreadable files are reported. This is an inventory, not an analyzer run.</p><div class="note"><strong>Read access only</strong><br>Source text and file metadata are read. No project scripts, dependency installation, or source writes are performed.</div><label class="check-row mt"><input type="checkbox" id="scope-consent" ${scopeAcknowledged?'checked':''}>I approve read access to this directory for this scan.</label><div class="modal-footer">${btn('Back','source')}<button class="primary" data-action="start-scan" ${scopeAcknowledged?'':'disabled'}>Scan codebase</button></div></section></div>`}
function scanning(){return `<div class="center-stage"><section class="progress-card"><div class="row spread"><span class="pill accent">Inventory running</span>${icon('folder')}</div><h2 class="mt">Reading codebase files</h2><p class="small mono">C:\\dev\\codebase-inspector</p><div class="progress-track" role="progressbar" aria-label="Scanning; total work not yet known"><div class="pulse"></div></div><div class="scan-stage">${icon('check')} Directory validated</div><div class="scan-stage active">${icon('clock')} Reading included files</div><div class="scan-stage">${icon('box')} Build directory layout</div><div class="scan-counters"><div><strong>96</strong><small>Files read so far</small></div><div><strong>12</strong><small>Excluded directories</small></div></div><div class="small" role="status">Current: <span class="mono">src/visualization/three/camera.ts</span><br>Total work is still being discovered.</div><div class="rule"></div><div class="row spread"><span class="small">Cancellation keeps the last complete snapshot.</span>${btn('Cancel scan','cancel-scan')}</div></section></div>`}
function tableRows(){return DESIGN_DATA.files.filter(f=>!query||f.path.toLowerCase().includes(query.toLowerCase())).slice(0,11).map(f=>`<tr class="${f.id===selected?'selected':''}"><td><button class="subtle mono" data-file-id="${f.id}">${esc(f.path)}</button></td><td>${f.lines}</td><td>${(f.bytes/1024).toFixed(1)} KiB</td><td>${f.category}</td></tr>`).join('')}
function fallback(){return toolbar()+`<div class="banner">${icon('alert')}<div><strong>The 3D view is unavailable.</strong> File inspection still works.<br><span class="small">Use the searchable list to access the same file measurements.</span></div>${btn('Retry 3D','city')}</div><div class="workspace-content"><div class="table-wrap"><div class="row spread mb"><h2>Codebase files</h2><span class="pill">144 included</span></div><table class="data-table"><thead><tr><th>Relative path</th><th>Physical lines</th><th>File size</th><th>Category</th></tr></thead><tbody>${tableRows()}</tbody></table><p class="small mt">Illustrative first 11 rows. In production the complete result list is paginated or accessibly virtualized.</p></div>${inspector()}</div>`+footer()}
function noResults(){return toolbar()+`<div class="filter-strip">Search: <span class="pill mono">${esc(query)}</span><span>0 of 144 files match</span></div><div class="center-stage"><div class="empty-results">${icon('search')}<h2 class="mt">No matching files</h2><p>The snapshot still contains 144 files. No paths match “${esc(query)}”.</p>${btn('Clear search','clear-search','primary')}<p class="small mt">Try a filename, extension, or part of its relative path.</p></div></div>`+footer()}
function settings(){return `<div class="settings-layout"><aside class="settings-nav"><div>Editor</div><div>Files and links</div><div>Appearance</div><div>Hotkeys</div><div>Community plugins</div><div class="active">Codebase Inspector</div></aside><div class="settings-content"><div class="overline">Community plugin settings</div><h1>Codebase Inspector</h1><p>Source profiles, inventory limits, and local storage.</p><div class="section-label">Codebase profiles</div><div class="card"><div class="row spread"><div><strong>Codebase Inspector</strong><p class="mono">C:\\dev\\codebase-inspector</p></div>${btn('Edit source','source')}</div><span class="pill">External · this machine</span></div><div class="setting-row"><div><strong>Inventory exclusions</strong><p>Skip dependencies, generated output, secrets, and configured paths.</p></div>${btn('Review','scope')}</div><div class="setting-row"><div><strong>Follow symbolic links</strong><p>Unavailable in the first release. Symlinks and junctions are skipped.</p></div><span class="pill">Off</span></div><div class="setting-row"><div><strong>Large text file limit</strong><p>Files above the limit retain metadata; physical lines are unavailable.</p></div><select aria-label="Maximum text file size"><option>2 MiB</option><option>5 MiB</option></select></div><div class="setting-row"><div><strong>Theme</strong><p>Controls and city surfaces follow Obsidian’s active theme.</p></div><span class="pill">Managed by Obsidian</span></div><div class="setting-row"><div><strong>Saved source binding</strong><p>The absolute path is a local binding, not part of portable snapshot identity.</p></div>${btn('Clear binding','clear-binding')}</div><div class="setting-row"><div><strong>Stored inspection data</strong><p>Small settings and local snapshots are separate. No source text is stored by default.</p></div>${btn('View storage','storage')}</div><div class="note neutral mt">No automatic scanning, analyzer installation, or network upload. Closing settings does not start work.</div></div></div>`}
function provider(){return toolbar()+`<div class="center-stage"><section class="welcome-card"><div class="overline">Add evidence · WP-02</div><h1>Connect fallow</h1><p class="intro">Keep exploring the structural city while you add analysis. Choose where the evidence comes from.</p><button class="option-card selected" data-action="import-demo"><span class="option-icon">${icon('file')}</span><span><strong>Import an existing report</strong><small>Validate a local JSON artifact. No analyzer is executed.</small></span><span class="radio"></span></button><button class="option-card" data-action="execution-info"><span class="option-icon">${icon('play')}</span><span><strong>Use an installed analyzer</strong><small>Choose a trusted executable and review its run scope.</small></span><span class="radio"></span></button><div class="note neutral mt">The plugin does not download or install fallow. Imported reports cannot authorize commands or source access.</div><div class="rule"></div><div class="row spread"><span class="small">Current structural snapshot: 144 files</span>${btn('Import report','import-demo','primary')}</div></section></div>`+footer()}
function workbench(){return toolbar({lens:true})+modes('workbench')+`<div class="workbench"><aside class="finding-queue"><div class="panel-heading">FINDINGS <span>12</span></div><div class="row mb"><span class="pill">All</span><span class="pill">Current report</span></div>${[['Cognitive complexity','src/visualization/layout/city-layout.ts','Reported: 27'],['Unused export','src/domain/schema.ts','No static consumers reported'],['Duplicate block','src/ui/InspectorPanel.vue','Clone group · 3 locations'],['Unused export','src/adapters/report-parser.ts','Review entry points'],['Complexity finding','src/application/scan-coordinator.ts','Reported: 22']].map(([t,p,m],i)=>`<button class="finding-item ${i===0?'active':''}" data-action="finding-details"><strong>${t}</strong><small class="mono">${p}</small><div class="small mt">${m}</div></button>`).join('')}</aside><section class="finding-evidence"><div class="overline">Finding F-001</div><h2>Complex branching in partitionDistrict</h2><div class="evidence-meta"><span class="pill warn">Cognitive complexity · 27</span><span class="pill">Threshold · 15</span></div><div class="path mono">src/visualization/layout/city-layout.ts:84</div><pre><span class="line-no">84 </span> function partitionDistrict(files, bounds) {\n<span class="line-no">85 </span>   if (files.length === 0) return [];\n<span class="line-no">86 </span>   const ordered = sortFiles(files);\n<span class="line-no">87 </span>   for (const file of ordered) {\n<span class="line-no">88 </span>     if (fitsCurrentRow(file, bounds)) {\n<span class="line-no">89 </span>       appendToRow(file);\n<span class="line-no">90 </span>     } else {\n<span class="line-no">91 </span>       startNextRow(file);\n<span class="line-no">92 </span>     }\n<span class="line-no">93 </span>   }\n<span class="line-no">94 </span> }</pre><p class="small">Illustrative excerpt, not enough to recompute the reported metric. Production excerpts require source access and an exact content match.</p><div class="section-label">Evidence and interpretation</div><div class="evidence-box"><strong>What the report supports</strong><p>This function exceeds the provider’s configured cognitive-complexity threshold.</p></div><div class="evidence-box"><strong>What remains unknown</strong><p>Whether a refactor improves the design; whether behavior is adequately tested; whether any change is safe to merge.</p></div><div class="note neutral">No automatic deletion or “safe to refactor” confidence score.</div></section><aside class="inspector"><h3>Evidence provenance</h3><div class="metric-row"><span>Provider</span><strong>fallow</strong></div><div class="metric-row"><span>Input</span><strong>Imported report</strong></div><div class="metric-row"><span>Scope</span><strong>Full configured root</strong></div><div class="metric-row"><span>Snapshot</span><strong class="mono">demo-09-17</strong></div><p class="small mt">Source match verified in this fixture. Provider version and config digest are preserved in the real report.</p><div class="section-label">Next action</div>${btn('Create investigation note','note','primary full','note')}<p class="small mt">Capture the evidence, your questions, and verification steps in this vault.</p>${btn('Focus file in city','selected','full','box')}</aside></div>`+footer()}
function noteModal(){return `<div class="modal-backdrop"><section class="modal large" role="dialog" aria-modal="true" aria-labelledby="dialog-title"><div class="modal-header"><h2 id="dialog-title">Create investigation note</h2>${ib('close','Close note composer','workbench')}</div><p>This action creates a Markdown note in the current vault. It does not change the codebase.</p><div class="columns"><div><div class="form-field"><label for="note-title">Title</label><input id="note-title" value="Investigate layout complexity"></div><div class="form-field"><label for="note-folder">Destination folder</label><input id="note-folder" value="Investigations"></div><div class="form-field"><label for="note-question">Investigation question</label><textarea id="note-question" rows="4">Can district partitioning be simplified while preserving deterministic placement?</textarea></div><label class="check-row"><input type="checkbox" checked>Include evidence and a focused view reference</label><label class="check-row mt"><input type="checkbox">Include a source excerpt</label><p class="small">Absolute machine paths and source text are excluded by default.</p></div><div class="note-preview"><div class="overline">Markdown preview</div><h3>Investigate layout complexity</h3><p class="small">status: open<br>codebase: demo-inspector<br>snapshot: demo-2026-09-17<br>finding: F-001</p><h3>Evidence</h3><p>Cognitive complexity 27, threshold 15.<br><code>src/visualization/layout/city-layout.ts:84</code></p><h3>Verification</h3><p>□ Review full function<br>□ Add behavioral tests<br>□ Compare deterministic layout</p><a href="#" data-action="selected">Open focused city →</a></div></div><div class="modal-footer">${btn('Cancel','workbench')}${btn('Create note','create-note','primary','note')}</div></section></div>`}
function compare(){return toolbar()+modes('compare')+`<div class="content-page"><div class="page-heading"><div><div class="overline">Snapshot comparison · WP-05</div><h2>See what changed, without losing your place</h2><p>Matching profile, scope, and provider configuration. Layout anchors are shared.</p></div><span class="pill ok">Compatible</span></div><div class="compare-canvas"><div class="compare-cell"><div class="compare-label"><strong>Baseline · 16 Sep, 13:00</strong><span>144 files</span></div>${CITY_ISO}</div><div class="compare-cell"><div class="compare-label"><strong>Current · 17 Sep, 13:00</strong><span>144 files · 3 changed</span></div>${changedCity()}</div></div><div class="cards-grid mt"><div class="card"><span class="small">Changed files</span><div class="big">3</div><p>Same identity; different content.</p></div><div class="card"><span class="small">Introduced findings</span><div class="big">2</div><p>Comparable provider runs.</p></div><div class="card"><span class="small">No longer reported</span><div class="big">1</div><p>Not inferred from a missing provider.</p></div></div><div class="lower-legend mt"><strong>Colors retain their original meaning.</strong><p>Changed files receive an outline. Missing, excluded, and deleted files are separate change types. No timeline automatically reruns analysis.</p></div></div>`+footer()}
function overview(){return toolbar()+modes('overview')+`<div class="content-page"><div class="page-heading"><div><div class="overline">Current evidence · WP-09</div><h1>Where to investigate next</h1><p>144 files · snapshot 17 Sep 2026, 13:00 · priorities explain their evidence.</p></div><span class="pill">No aggregate health score</span></div><div class="cards-grid"><div class="card"><span class="small">Reported findings</span><div class="big">12</div><p>5 complexity · 4 unused · 3 clone groups</p></div><div class="card"><span class="small">Introduced findings</span><div class="big">2</div><p>Versus a compatible baseline</p></div><div class="card"><span class="small">Coverage availability</span><div class="big">108 / 144</div><p>Files with measured coverage</p></div></div><div class="columns mt"><section class="card"><div class="row spread"><h3>Investigation queue</h3><span class="small">Evidence, not a score</span></div><ol class="score-list"><li><span class="priority-index">1</span><div><strong>city-layout.ts</strong><p>New complexity finding · measured branch coverage 50%</p>${btn('Inspect evidence','workbench','subtle')}</div></li><li><span class="priority-index">2</span><div><strong>scan-coordinator.ts</strong><p>Existing complexity finding · changed in this snapshot</p></div></li><li><span class="priority-index">3</span><div><strong>schema.ts</strong><p>Unused-export candidate · consumer review required</p></div></li></ol></section><section class="card"><h3>Available evidence</h3><div class="metric-row"><span>Filesystem inventory</span><strong>Complete</strong></div><div class="metric-row"><span>Fallow</span><strong>Imported</strong></div><div class="metric-row"><span>Coverage</span><strong>108 files measured</strong></div><div class="metric-row"><span>Runtime</span><strong>Not connected</strong></div><p class="small mt">Unavailable providers are not counted as zero issues. Each metric links to its records and scope.</p>${btn('Open city','city','full mt','box')}</section></div></div>`+footer()}
function embed(){return `<div class="content-page"><article class="note-document"><div class="overline">Investigation note · WP-11</div><h1>Investigate layout complexity</h1><div class="property-grid"><span class="muted">Status</span><span>Open</span><span class="muted">Codebase</span><span>demo-inspector</span><span class="muted">Snapshot</span><span class="mono">demo-2026-09-17</span><span class="muted">Finding</span><span class="mono">F-001</span></div><h2>Question</h2><p>Can district partitioning be simplified while preserving deterministic placement?</p><div class="embed-city"><div class="embed-head"><strong>city-layout.ts · pinned snapshot</strong><a href="#" data-action="selected">Open focused city →</a></div>${CITY_ISO}<div class="embed-head"><span>Static preview · rendering paused</span><span>No scan on note open</span></div></div><h2>Evidence</h2><p>The imported report identifies cognitive complexity of 27 at a configured threshold of 15. Inspect the full function before proposing a change.</p><div class="note neutral"><code>src/visualization/layout/city-layout.ts:84</code><br>Evidence is pinned to the snapshot. A newer scan does not rewrite this note.</div><h2>Verification</h2><p>□ Review the full function and its consumers.<br>□ Add behavioral tests for empty and constrained districts.<br>□ Confirm deterministic placement after the change.</p></article></div>`}
function security(){return toolbar()+`<div class="content-page"><div class="page-heading"><div><div class="overline">Diagnostics and security · WP-07</div><h1>Different signals. Explicit scope.</h1><p>Original rule IDs and severities remain visible. Synthetic examples, not real advisories.</p></div><span class="pill">2 providers</span></div><div class="cards-grid"><div class="card"><span class="small">Lint diagnostics</span><div class="big">8</div><p>ESLint report · source locations</p></div><div class="card"><span class="small">Dependency advisories</span><div class="big">1</div><p>Exact package version in lockfile</p></div><div class="card"><span class="small">Exploitability</span><div class="big" style="font-size:22px">Not assessed</div><p>No claim inferred from an advisory</p></div></div><table class="data-table mt"><thead><tr><th>Record</th><th>Target</th><th>Native severity</th><th>Evidence</th></tr></thead><tbody><tr><td>no-unused-vars</td><td class="mono">src/adapters/fallow.ts:62</td><td>Warning</td><td>ESLint</td></tr><tr class="selected"><td>DEMO-ADVISORY-001</td><td class="mono">example-parser@1.2.3</td><td>High · reported</td><td>Dependency report</td></tr><tr><td>no-explicit-any</td><td class="mono">src/domain/metric.ts:18</td><td>Warning</td><td>ESLint</td></tr></tbody></table><div class="columns mt"><div class="card"><h3>Package evidence</h3><p>Lockfile match: example-parser@1.2.3<br>Manifest location: package-lock.json<br>Finding identity: DEMO-ADVISORY-001</p></div><div class="note neutral"><strong>Do not paint every source file as vulnerable.</strong><br>Package presence, source reachability, and exploitable behavior require different evidence.</div></div></div>`+footer()}
function bundle(){return toolbar()+`<div class="content-page"><div class="page-heading"><div><div class="overline">Build artifact evidence · WP-08</div><h1>What contributes to the shipped plugin?</h1><p>Imported build report · build demo-9d41 · rendered bytes</p></div><select aria-label="Bundle byte metric"><option>Rendered bytes</option><option>Minified bytes</option></select></div><div class="cards-grid"><div class="card"><span class="small">Output chunk</span><div class="big">main.js</div><p>One selected build artifact</p></div><div class="card"><span class="small">Rendered size</span><div class="big">920 KiB</div><p>Source bytes are not this metric</p></div><div class="card"><span class="small">Compression evidence</span><div class="big" style="font-size:22px">Unavailable</div><p>Not estimated from module sizes</p></div></div><div style="display:grid;grid-template-columns:3fr 2fr 1.5fr;gap:8px;min-height:210px;margin-top:20px"><div class="card" style="background:#dbe6ef;color:#233749"><h3>three</h3><p style="color:inherit">460 KiB · rendered contribution</p></div><div class="stack"><div class="card" style="flex:1;background:#e7dff1;color:#443552"><h3>Vue runtime</h3><p style="color:inherit">180 KiB</p></div><div class="card" style="background:#dae8df;color:#2d4435"><h3>Application</h3><p style="color:inherit">120 KiB</p></div></div><div class="card"><h3>Other modules</h3><p>160 KiB</p></div></div><div class="columns mt"><div class="card"><h3>Selected source contribution</h3><p class="mono">src/visualization/layout/city-layout.ts → main.js</p><div class="metric-row"><span>Rendered bytes</span><strong>8.4 KiB</strong></div><div class="metric-row"><span>Source bytes</span><strong>11.0 KiB</strong></div></div><div class="note neutral"><strong>Size is not execution performance.</strong><br>Bundle relationships come from the imported build, not from the source import graph alone. Shared modules are deduplicated by artifact identity.</div></div></div>`+footer()}
function artifacts(){return toolbar()+`<div class="content-page"><div class="page-heading"><div><div class="overline">Local updates and CI artifacts · WP-10</div><h1>Update evidence deliberately</h1><p>Opening a view or an imported report never authorizes execution.</p></div><span class="pill">Watch is off</span></div><div class="columns"><section class="card"><h3>Import a CI snapshot</h3><p>A portable artifact can be inspected without access to the original source root.</p><div class="form-field"><label for="artifact-path">Artifact path</label><input id="artifact-path" value="artifacts/codebase-snapshot.json"></div><div class="metric-row"><span>Schema</span><strong>Supported</strong></div><div class="metric-row"><span>Original source</span><strong>Not bound locally</strong></div><div class="metric-row"><span>Source excerpt</span><strong>Not included</strong></div>${btn('Review imported artifact','import-demo','primary full mt')}</section><section class="card"><h3>Explicit watch session</h3><p>Watch one approved codebase. Changes trigger a debounced inventory only while the session is active.</p><div class="note neutral">No analyzer execution is authorized by this control. That needs its own approved run profile.</div><div class="metric-row"><span>Source</span><strong>demo-inspector</strong></div><div class="metric-row"><span>Session</span><strong>Not started</strong></div>${btn('Review and start watch','watch','full mt')}</section></div><div class="card mt"><h3>Jobs and evidence</h3><table class="data-table"><thead><tr><th>Job</th><th>Collection result</th><th>Quality verdict</th><th>Publication</th></tr></thead><tbody><tr><td>Inventory</td><td>Complete</td><td>Not applicable</td><td>Snapshot 13:00</td></tr><tr><td>Fallow import</td><td>Valid report</td><td>Findings reported</td><td>Imported evidence</td></tr><tr><td>Watch session</td><td>Not running</td><td>Not applicable</td><td>No pending update</td></tr></tbody></table></div></div>`+footer()}
function render(){clearTimeout(timer);let slug=screen.slug;document.body.classList.toggle('light',screen.theme==='light');let main='';switch(slug){case'welcome':main=welcome();break;case'source':main=welcome()+sourceModal();break;case'scope':main=welcome()+scopeModal();break;case'scanning':main=scanning();break;case'fallback':main=fallback();break;case'settings':main=settings();break;case'provider':main=provider();break;case'workbench':main=workbench();break;case'note':main=workbench()+noteModal();break;case'compare':main=compare();break;case'overview':main=overview();break;case'no-results':main=noResults();break;case'embed':main=embed();break;case'security':main=security();break;case'bundle':main=bundle();break;case'artifacts':main=artifacts();break;default:main=cityContent();}$('#artboard').innerHTML=host(main);$('#scenario-select').value=screen.id;updateSelection();setupScene();setupDialog();document.title=screen.id+' · '+screen.title+' — Codebase Inspector design';}
function navigate(target){let next=DESIGN_SCREENS.find(s=>s.slug===target||s.id===target);if(!next)return;screen=next;query=target==='search'||target==='S08'?'layout':target==='no-results'||target==='S23'?'legacy-payment':'';topMode=screen.slug==='top';inspectorClosed=['city','search','top'].includes(screen.slug);selectionActive=!inspectorClosed;zoom=1;pan={x:0,y:0};help=false;try{history.replaceState({},'',location.pathname+'?screen='+screen.id+(document.body.classList.contains('capture')?'&capture=1':''));}catch(_){/* Sandboxed review still supports local state transitions. */}render()}
function updateSelection(){ $$('.building').forEach(b=>{b.classList.toggle('selected',b.dataset.file===selected&&selectionActive&&screen.slug!=='city');let f=DESIGN_DATA.files.find(f=>f.id===b.dataset.file);b.classList.toggle('dim',Boolean(query&&f&&!f.path.toLowerCase().includes(query.toLowerCase())));});$$('.file-row').forEach(b=>b.classList.toggle('selected',b.dataset.fileId===selected&&selectionActive));let sh=$('#scene-host');if(sh)sh.style.transform=`translate(${pan.x}px,${pan.y}px) scale(${zoom})`;}
let drag=null,dragged=false;
function setupScene(){let h=$('#scene-host');if(!h)return;h.addEventListener('pointerdown',e=>{if(e.button!==0)return;drag={x:e.clientX,y:e.clientY,ox:pan.x,oy:pan.y};dragged=false;h.setPointerCapture(e.pointerId)});h.addEventListener('pointermove',e=>{if(!drag)return;let dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.hypot(dx,dy)>5)dragged=true;if(dragged){pan={x:drag.ox+dx,y:drag.oy+dy};updateSelection()}});h.addEventListener('pointerup',e=>{if(!dragged){let hit=document.elementFromPoint(e.clientX,e.clientY)?.closest('.building');if(hit){selected=hit.dataset.file;selectionActive=true;inspectorClosed=false;render()}}drag=null;});h.addEventListener('dblclick',()=>{zoom=1.25;updateSelection()});}
function setupDialog(){let modal=$('[role=dialog]');if(!modal)return;modal.addEventListener('keydown',e=>{if(e.key==='Tab'){let focusables=$$('button,input,textarea,select,a[href]',modal).filter(x=>!x.disabled);let first=focusables[0],last=focusables.at(-1);if(e.shiftKey&&document.activeElement===first){last.focus();e.preventDefault()}else if(!e.shiftKey&&document.activeElement===last){first.focus();e.preventDefault()}}});if(!document.body.classList.contains('capture'))setTimeout(()=>$('input,button',modal)?.focus(),10)}
function toast(text){$('.toast')?.remove();let el=document.createElement('div');el.className='toast';el.role='status';el.textContent=text;$('#plugin-view').append(el);setTimeout(()=>el.remove(),5000)}
$('#scenario-select').innerHTML=DESIGN_SCREENS.map(s=>`<option value="${s.id}">${s.id} · ${s.title} · WP-${String(s.package).padStart(2,'0')}</option>`).join('');$('#scenario-select').onchange=e=>navigate(e.target.value);$('#preview-theme').onclick=()=>{screen={...screen,theme:screen.theme==='light'?'dark':'light'};render()};
document.addEventListener('input',e=>{if(e.target.id==='file-search'){query=e.target.value;updateSelection();let panel=$('.file-panel');if(panel){let temp=document.createElement('div');temp.innerHTML=listPanel();panel.replaceWith(temp.firstElementChild)}let body=$('.table-wrap tbody');if(body)body.innerHTML=tableRows()}if(e.target.id==='scope-consent'){scopeAcknowledged=e.target.checked;$('[data-action="start-scan"]').disabled=!scopeAcknowledged}});
document.addEventListener('change',e=>{if(e.target.id==='lens-select'){navigate(({category:'selected',findings:'findings',coverage:'coverage',runtime:'runtime'})[e.target.value])}});
document.addEventListener('click',e=>{let src=e.target.closest('[data-source]');if(src){sourceKind=src.dataset.source;render();return}let file=e.target.closest('[data-file-id]');if(file){selected=file.dataset.fileId;selectionActive=true;inspectorClosed=false;render();return}let el=e.target.closest('[data-action]');if(!el)return;e.preventDefault();let a=el.dataset.action;if(['city','source','scope','selected','settings','provider','workbench','note','compare','overview','fallback','embed','artifacts'].includes(a)){navigate(a);return}switch(a){case'scan':navigate('scope');break;case'start-scan':if(scopeAcknowledged){navigate('scanning');timer=setTimeout(()=>{navigate('selected');toast('Simulated scan complete. No files were read.');},1600)}break;case'cancel-scan':navigate('cancelled');break;case'clear-search':query='';navigate('selected');break;case'clear-selection':selectionActive=false;inspectorClosed=true;render();break;case'close-inspector':inspectorClosed=true;render();break;case'fit':zoom=1;pan={x:0,y:0};updateSelection();break;case'focus':zoom=1.25;pan={x:0,y:0};updateSelection();break;case'zoom-in':zoom=Math.min(1.8,zoom+.15);updateSelection();break;case'zoom-out':zoom=Math.max(.6,zoom-.15);updateSelection();break;case'top':topMode=!topMode;render();break;case'help':help=!help;render();break;case'copy-path':navigator.clipboard?.writeText(selectedFile().path).then(()=>toast('Relative path copied.')).catch(()=>toast(selectedFile().path));break;case'import-demo':navigate('findings');toast('Demo report loaded. No file was read or analyzer executed.');break;case'execution-info':toast('Production: review executable path, version, arguments, working directory, side effects, and cancellation. This prototype cannot execute analyzers.');break;case'create-note':navigate('embed');toast('Simulated note preview. Nothing was written to your vault.');break;case'watch':toast('Prototype only. A real watch session requires explicit source approval; no watcher has started.');break;case'clear-binding':toast('Prototype only. Production requires a confirmation; source files must remain unchanged.');break;case'storage':toast('Design: store snapshots separately from settings; exclude outputs from scans; source text off by default.');break;case'edge-list':toast('Outgoing: city-layout.ts → snapshot.ts, identity.ts, partition-district.ts, bounds.ts, layout-types.ts, sort-files.ts. Synthetic fixture.');break;case'finding-details':toast('The review fixture contains one detailed finding. Production selection loads each finding’s own evidence.');break;case'noop':toast('Illustrative Obsidian host chrome; not a plugin control.');break;}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(help){help=false;render();return}if($('[role=dialog]')){navigate(screen.slug==='note'?'workbench':'city');return}if(query){query='';render();return}inspectorClosed=true;render();return}if(e.key==='/'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName)){$('#file-search')?.focus();e.preventDefault()}});
query=screen.slug==='search'?'layout':screen.slug==='no-results'?'legacy-payment':'';topMode=screen.slug==='top';inspectorClosed=['city','search','top'].includes(screen.slug);selectionActive=!inspectorClosed;render();
