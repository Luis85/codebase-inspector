---
project: codebase-inspector
title: WP-04 Part 1 — SDD ledger (rulings)
date: 2026-09-25
branch: feat/wp-04-part1
---

# WP-04 Part 1 — rulings

This ledger records every ruling made while planning and executing WP-04 Part 1 (Investigation workbench and Markdown notes), and what each one costs if it is wrong.

- Spec: `docs/superpowers/specs/2026-09-25-wp04-part1-investigation-design.md` (IN1–IN51, owner decisions O1–O8).
- Plan: `docs/superpowers/plans/2026-09-25-wp04-part1-investigation.md` (19 tasks: Task 0, the native harness, then Tasks 1–18).
- Branch: `feat/wp-04-part1` from `1cba08c`, the PR 1 head after the WP-03 Part 1 polish pass (polish ledger JP8).
- Precedent, all binding: the Part 3–7 ledgers, the Part 7 polish ledger, the WP-03 Part 1 ledger (J, JF, "WP-03 E") and the WP-03 Part 1 polish ledger (PO, JP, JQ, "WP-03 Polish E").
- Numbering:
  - **O1…** are owner decisions;
  - **IN1…** are the spec's decisions;
  - **IP1…** are planning rulings;
  - **IPF1…** are pre-flight rulings;
  - **"WP-04 E1…"** are execution rulings.
- Every ruling reads: **Ruling:** what — why — cost if wrong.

## Owner decisions (2026-09-25, before the spec)

| # | Decision |
|---|---|
| O1 | Part 1 covers all of 04.1–04.7. Clone groups and symbol tracing are deferred. |
| O2 | A new **Investigate** screen (route). Quality, File detail, the city file inspector and Architecture open it on the same finding. |
| O3 | **Notes are the record.** A note is linked to its finding by its own frontmatter, read through Obsidian's metadata cache. No link table; the review format stays v2. |
| O4 | **A folder setting plus a confirm dialog.** Nothing is written until the user confirms the folder and filename. |
| O5 | **No external-editor action** in Part 1. |
| O6 | **Independent status.** The note's `status` is the user's; nothing syncs with review dispositions or work items. |
| O7 | **Notes may be written inside the codebase root**, after an explicit checkbox that adds the folder to the scan exclusions; the README and the two storage disclosures are reworded. |
| O8 | **Native Obsidian e2e testing** (2026-09-25), adopted from `github.com/Luis85/describe` (MIT, same author) as **WP-04 Task 0**, to make e2e testing easy: Vitest drives a real installed Obsidian through standalone WebdriverIO and `wdio-obsidian-service` (spec IN42–IN51). If a native session cannot start on this Windows machine, the fallback is the in-memory path already planned. |

## Planning rulings

| # | Ruling |
|---|---|
| IP1 | **Ruling:** 19 tasks since O8: Task 0 (the native harness, which also runs the §6 probes) first, then 18 tasks in dependency order: route and store skeleton (1) → pure text, names and folders (2) → note model and splice (3) → stale rule and preview (4) → index reducer (5) → read model (6) → fake vault and YAML mock (7) → folder setting (8) → notes port and host (9) → wiring and Settings row (10) → screen (11) → preview panel (12) → create (13) → refresh and orphans (14) → entry points (15) → spine and safety (16) → harness (17) → evidence (18). Tasks 2–5 have no UI dependency and can run in any order after Task 1. — Every task ends with its own test cycle, and the pure application code is done and reviewed before any host or UI code consumes it. — Low: a mis-ordered dependency shows in the controller's "Consumes" re-check. |
| IP2 | **Ruling:** `mdCode` moves verbatim from `src/ui/export/markdown.ts` to `src/application/markdown-code.ts`; `markdown.ts` re-exports it so `report.ts`, `work-items.ts` and `tests/unit/markdown.test.ts` do not change. — IN24 says paths go "through the existing `mdCode()`", but the note renderer is application code, which may not import UI. A second copy would be a fallow duplication finding. — None. |
| IP3 | **Ruling:** the application layer holds no copy. The note's headings, prompts and labels are one `NOTE_VOCABULARY: NoteVocabulary` in `src/ui/audit-copy/investigation.ts`, passed as an argument to `renderEvidenceBlock`/`renderNoteBody`. The UI renders the body and block text and hands the text to the port; the host writes it and checks only the markers. — The precedent is `normalize-fallow.ts` ("application code holds no copy"; the UI labels `notShown` keys), and IN22's headings are user-facing words. — Low: the host trusts in-process text, which it validates structurally (`isEvidenceBlock`) before any write. |
| IP4 | **Ruling (corrects IN20, S2):** `finding_fingerprint` is written as the portable key `<source_path>#<finding_id>` (the review export's `findingRef`, `src/ui/read-models/review-state.ts:64`), and `entity_id` as `file:<source_path>`. The index maps notes by that key; a row finds its notes through `findingRef(row.fingerprint)`. — The in-memory fingerprint and entity id are NUL-joined (`src/domain/entity-id.ts:4`), so writing them puts NUL bytes into YAML and ties a note to a repository id format; the deliverable asks for "portable identifiers". — Low: `entity_id` duplicates `source_path`; it stays because the deliverable's model names it. |
| IP5 | **Ruling (extends IN24, S5):** `noteText` also flattens every line break (CR, LF, U+2028, U+2029) to a space, turns tabs into spaces, replaces C0/C1 controls and bidi/format controls (U+200E/F, U+202A–E, U+2066–9) with U+FFFD, escapes `:` everywhere and the `.` of `www.`, escapes a leading `N)` as well as `N.`, and caps each value at 256 code points before escaping. — IN24 escapes a leading `-` only at the string's start, so a symbol holding `\n# x` would start a heading; bare `https://` and `www.` are auto-linked by Obsidian; `1)` is a CommonMark list opener; bidi controls hide text (Trojan Source). — Low: notes show `\:` in source view; reading view renders `:`. |
| IP6 | **Ruling (makes IN25 hold):** the evidence block renders through a shrink ladder — (50 list entries, 256 code points per value), then (20, 256), (5, 128), (0, 64) — and returns the first result at or under 16 KiB UTF-8; the last step is within the bound for any input, pinned by a worst-case test. — Several report strings are unbounded (`export_name`, `name`, `severity`, `fallow-report-schema.ts:50,115,122`), so a 50-entry cap alone bounds nothing. — Low: a pathological finding's note shows fewer entries and shorter values than a normal one. |
| IP7 | **Ruling (sharpens IN23/IN31):** a marker is a whole line equal to the marker text; only a CR before the LF is tolerated, so an indented or trailing-space marker counts as edited. The new block takes the begin line's line ending (CRLF notes stay CRLF); everything outside the two marker lines is returned byte for byte, including an end marker on the last line with no newline. — "Exactly one begin followed by exactly one end" needs a line definition, and a tolerant match would let a marker inside prose or a code span count. — Low: a user who indents a marker must restore it before refreshing (the dialog says so). |
| IP8 | **Ruling (corrects IN38, S3):** after a refresh the human body sections are byte-identical, and the user's frontmatter keys (and `status`, `created`) are **value**-identical (compared parsed). — IN32 mandates `processFrontMatter`, which re-serialises the whole YAML block, so bytes (comments, quoting, key order of user keys) cannot be promised. — Medium: a user's YAML comments in the frontmatter are lost on refresh; recorded as a limitation. |
| IP9 | **Ruling (extends IN21/IN27, S7):** names are cut by code point; trailing dots and spaces are stripped; a Windows reserved stem is suffixed with `_` with or without an extension (`CON` → `CON_`, `nul.backup` → `nul_.backup`). A name is taken when `getAbstractFileByPath` finds it **or** the folder has a child whose name matches case-insensitively; existing folder segments are matched case-insensitively and their own case is used. The dialog's name field holds the base name; `.md` is shown as a suffix. — Windows and macOS vaults are case-insensitive while `getAbstractFileByPath` is not, and Windows silently drops trailing dots, so both would pass the pre-check and fail at `vault.create`. — Low: on a case-sensitive Linux vault, `a.md` and `A.md` get distinct names anyway (`A (2).md`). |
| IP10 | **Ruling (sharpens IN19):** folder problems are `empty`, `not-relative` (anything `normalizeRelativePath` refuses), `too-long` (over 200 code points), `config-dir` (the config folder or inside it, case-insensitive) and `unsafe-name` (a segment `sanitizeNoteName` would change); trailing slashes are dropped first; the vault root itself is not a valid folder. A stored folder is validated by shape only when read (`{ folder: string(1..200) }`), and fully when planned. Editing the folder in the create dialog affects that note only, never the setting. — The spec lists the rules but not the words, the root, or what a hand-edited `data.json` does. — Low: a hand-edited invalid folder shows its problem in the dialog, where it can be corrected for that note. |
| IP11 | **Ruling:** the default folder is `Codebase investigations/<sanitised profile name>`, or `Codebase investigations` when the name sanitises to nothing or the profile is gone. — IN18 does not say what an empty name gives. — None. |
| IP12 | **Ruling (extends §3, S9):** `InvestigationNotesPort` has eight members: the spec's five plus `destination(codebaseId)` (the folder and whether it is the default), `plan(folder, baseName, rootPath)` (validation, final name, overlap) and `sourceNotePath(rootPath, relativePath)` (IN12). The host implementation is split into `investigation-notes.ts` and `investigation-note-index.ts`, and registers its four events through `plugin.registerEvent` on first use, never in `onload`. — The dialog must show the final name and the overlap before confirm (IN26/IN27) without writing; the UI does not know profile names or the vault base path; and spec 4.4 forbids work in `onload`. — None. |
| IP13 | **Ruling (sharpens IN33):** the index is an incremental reducer. `changed`, `rename` and `delete` update one entry; `resolved` rebuilds from `getMarkdownFiles()`; the reducer returns the same map when nothing changed, so listeners hear only real changes. A note is ours when `type` is `codebase-investigation`; one whose `codebase_id` is not a non-empty string is ignored (it cannot be attributed); one whose other identity keys are not non-empty strings of at most 2,048 characters is malformed and counted for its codebase. Several notes for one finding are all listed, by path. — A full rebuild per `changed` event is O(vault) on every keystroke-driven cache update. — Low: a missed event is repaired at the next `resolved`. |
| IP14 | **Ruling (resolves IN7's inputs, S8):** the preview reads under the codebase's **live** binding root on this device (`profileStore.get` → `bindingStore.get`), and only when it equals the snapshot's `scope.rootPath` (segment-wise); otherwise the state is `no-binding` with words that say the folder is not connected or now points elsewhere. `maxBytes` is `min(snapshot.scope.maxFileBytes, 512 KiB)`. — The UI has no profile and no binding; after Reconnect to another folder, reading the snapshot's paths under the new root would show another file's lines as this finding's. — Low: a user who reconnects must rescan before previewing. |
| IP15 | **Ruling:** the preview stats before it reads: a missing parent or file is `missing`; a symbolic link or junction as the file **or any parent folder below the root** is `outside-root`; a non-file is `not-a-file`; a size over the limit is `too-large`. Only then does it call `readText`, and it classifies an `unavailable` reason by the adapter's own message prefixes (`file exceeds…`, `file appears to contain binary…`, `file is not valid UTF-8…`, else `read-error`), pinned against the real adapter by an integration test. — `readText` lstat's only the last segment and then follows links, and `SourceFileSystemPort` is a frozen §4.5 contract whose result carries only a free-text reason. — Low: an adapter message change turns a precise state into `read-error`, and the integration test fails first. |
| IP16 | **Ruling (sharpens IN8):** the window is 20 lines before and after, clipped at the file's edges (never shifted to fill 41); a line past the end shows the last 41 lines; no line shows the first 41. Lines split on LF with an optional CR (a lone CR stays and shows as `\u000D`); the line count is `countPhysicalLines`, the same function as the scan's observation. Lines are cut at 400 code points. C0 controls except tab, DEL, C1 and the bidi/format controls show as `\uXXXX`. — IN10's line check compares against the scan's count, so both must count the same way; bidi controls are the same hazard in a preview as in a note. — None. |
| IP17 | **Ruling (sharpens IN10):** the checks run in order report → size → lines → modified → line-range, and the verdict names the first that fails with a cause (`changed` or `unknown`); a finding without a line is its own verdict (`no-line`), never exact. An mtime equal to the analysis time passes. — "Naming the failed check" needs a defined order, and "unknown fails towards stale" needs a distinct cause for the words. — None. |
| IP18 | **Ruling (resolves IN6's contradiction, S11):** the finding list follows `CodebaseFileList.vue`'s model — native row buttons in a `<ul>`, one roving tab stop (`useRovingIndex`), arrows move focus and never select, Enter or click selects, `aria-current="true"` on the selected row — not `role="listbox"`. — IN6 says "a listbox pattern … as the file list", but the file list is not a listbox; selecting on focus would read a file on every arrow press, against IN13's "one read per selection". — Low: a screen-reader user hears buttons, not options. |
| IP19 | **Ruling (resolves IN2, S4):** the filters are free-text query, Type, Rule, Severity, Status and Note; Quality's Module filter is not carried. The default status is `all`. When an entry point selects a finding the current filter hides, the filter resets to the default so the selected row is listed. Rows page by `FINDINGS_PAGE` (100) with Show more. — Quality has no rule filter, and the spec names five filters; a row the user was sent to must be visible. — Low: a user who wants Module must use the query field. |
| IP20 | **Ruling:** selecting a finding in Investigate changes neither the city selection nor the camera; only Open file detail selects the anchor (and navigates). Entry points arrive with the city selection they already had. — IN4 forbids moving the camera but is silent on the selection, and a silent selection change would move the city inspector under the user. — None. |
| IP21 | **Ruling:** `'investigate'` is inserted in `ROUTE_IDS` before `'workbench'`; the Act nav reads Investigate, Refactor workbench, Audit report; the icon is `search` (already used in `src`, JQ1 precedent); `part: 4` (the Act group's WP-02 part; `RouteMeta.part` has no reader). Three tests that pin route and nav counts are edited in place (`route-state.test.ts:11`, `command-palette.test.ts:19`, `workspace-shell.test.ts:62`). — Investigating comes before planning work; `part`'s type is `1 \| 2 \| 3 \| 4`. — None. |
| IP22 | **Ruling (implements IN17, S13):** `WorkItemEditor.vue` gains an optional `draft: { title; notes }` prop used in create mode only. Investigate opens it with the title `Investigate <finding id> in <file name>` and plain-text notes (kind, rule, path, line; no link syntax). The one-refactor-item-per-file rule (Q4) is unchanged: a second finding on the same file shows In refactor plan, or the user picks another intent in the editor. — The editor has no pre-fill input (`WorkItemEditor.vue:24`). — Low: two findings on one file share a refactor item. |
| IP23 | **Ruling:** the evidence block holds the finding, its source, scope, time and uncertainties, and never the review disposition or the file's work items; before any preview is read, the line uncertainty says the line was not checked. — O6 makes the note's status independent of the plugin's review state, which can change without the note; a stale copy of it in the note would contradict the screen. — Low: a reader of the note alone does not see the plugin's disposition. |
| IP24 | **Ruling:** creating a note does not open it; the notes panel lists it and focus moves to its Open button. — Opening a new leaf moves focus out of the inspector mid-flow; the deliverable's "create/open" is two actions. — None. |
| IP25 | **Ruling (resolves IN31, S16):** the refresh dialog lists the snapshot change (old → new, from frontmatter), a source-path change, the current evidence state (current, stale or not reported) and the current line. It does not compare the old line or state. — The note stores neither (IN20), and parsing the old block for them would read user-editable text as data. — Low: the dialog says what the block will say, not what it said. |
| IP26 | **Ruling (sharpens IN29, S6):** overlap means only "the note folder, joined to the vault base path, is inside the binding root". Its root-relative path is added as an exclusion unless it, or a parent of it, is already excluded (`already`); a failed or impossible exclusion keeps the note and says so. A folder equal to the root offers no checkbox and says the note will appear in the next scan. — IN29's first clause ("the vault base path contains the binding root") does not put the notes inside the root; an empty exclusion would exclude everything. — Low: notes in a folder equal to the root are scanned. |
| IP27 | **Ruling:** removing a profile purges its `investigations` entry after its analyzer purge, with its own failure notice; notes stay in the vault. — Y17/Z11 purge a removed profile's settings; the non-goals forbid deleting notes. — None. |
| IP28 | **Ruling:** `CodebaseInspectorSettingTab` takes the folder store as a required 9th constructor parameter. The five test constructions are edited on their existing line with `createFakeInvestigationFolders()` (one import line each; `settings-tab.test.ts` 436 → 437). — J10's in-place rule for near-cap files; an optional parameter would let `main.ts` forget it. — None. |
| IP29 | **Ruling (resolves IN37, S17):** `yaml` 2.9.1 becomes an explicit devDependency (already installed as Vite's dependency, so nothing new is downloaded). The Obsidian mock's `parseYaml`/`stringifyYaml` wrap it, with `TFile`, `TFolder`, `TAbstractFile` and `Plugin.registerEvent`. No test asserts YAML bytes, only parsed values. — The mock has none of these today, and relying on a transitive package breaks when Vite changes its dependencies. Obsidian's own serialiser is not this library. — Low: a quoting difference in real Obsidian is found by the pre-flight probe, not by tests. |
| IP30 | **Ruling:** `tests/fixtures/fake-vault.ts` and `tests/fixtures/fake-investigation.ts` import neither `vitest` nor `node:*`. — Task 17's harness bundles them in a browser; WP-03 Task 14 broke every capture with one `node:fs` import. — None. |
| IP31 | **Ruling (the words for IN41; the substance is O7):** Task 8 sets `SETTINGS_STORAGE_TEXT` to "Work items, finding decisions and boundary rules are saved for each codebase in this plugin’s own data for this vault, so they survive restarts. Imported findings and the report screen’s sections and note are kept for this session only. Investigation notes are ordinary notes in your vault, written only when you create or refresh one." Task 8 keeps `STORAGE_DISCLOSURE_TEXT`'s first two sentences and replaces the rest with "Each codebase’s investigation notes folder is stored there too. Removing a profile removes its review decisions, its executable setting and its notes folder setting; it never deletes the investigation notes you created. fallow findings, imported or collected, are kept for this session only. Nothing about them is sent anywhere else." Task 18 replaces `README.md:16–17` with "- It reads **file text and file metadata only**. It never writes, moves, renames or deletes anything in the directory you select, with one exception you confirm each time: an investigation note you create in a folder inside that directory." — Each sentence says exactly what IN41 requires and nothing more; the README keeps its list form, and `tests/host/clean-vault-install.test.ts` reads only its other disclosures (out-of-vault reads, no network, no telemetry), which are unchanged. — Low: wording only; `settings-fallow.test.ts:179` pins the disclosure, so a later rewording is a one-line test edit. |
| IP32 | **Ruling:** `FILE_SOURCE_PREVIEW_LATER` ("Source preview arrives with the source provider.") is reworded in place to point at Investigate; File detail itself still reads no content (P9), so `FILE_SOURCE_SUBTITLE` stays. — The placeholder would be false once the preview ships. — None. |
| IP33 | **Ruling:** the city inspector's Findings section is a new component, `src/ui/screens/city/CityFindingsPanel.vue` (beside `CityRelationsPanel.vue`); `FileInspector.vue` gains two lines. It shows anchored findings first, then related ones, at most 10, then "N more on Investigate", and only while a report is attached. — `FileInspector.vue` is 215 lines and a component keeps it small; the order and the "more" text are unstated. — None. |
| IP34 | **Ruling:** File detail's finding rows get a **sibling** Investigate button after the row's Review button; Quality's review dialog gets one in its links row; Architecture's cycle rows and fallow boundary rows get one beside every Review finding (not only the selected row). All go through one `useOpenInvestigation()`. — E20 forbids a button inside a button; IN5's "selected cycle or violation" matches no current UI, where Review finding is on every row. — None. |
| IP35 | **Ruling:** the spine test lives in `tests/acceptance/` (jsdom), copies the WP-03 relations project to a temp folder, scans it with the real Node port, and reads the 3.27.0 recording with no strip prefix (its `src/…` paths match the scan). The source preview reads the real copied files. — IN38 asks for "the real relations and fallow recordings", and IN10's exact highlight needs real observations and mtimes. — Low: slower than a unit test (30 s timeout). |
| IP36 | **Ruling:** the harness gains `?investigate=demo\|stale\|create`, served by a scripted preview (sizes equal to, or one byte off, the harness snapshot's observations) and a fake-vault notes port holding one linked and one orphan note; the five captures use `screen=s05`. — The harness has no filesystem and no Obsidian vault; `s05` is the WP-03 Architecture captures' base state. — None. |
| IP37 | **Ruling:** the investigation store resets its selection, gone notice, preview, notes and destination synchronously (`flush: 'sync'`) when the bound codebase changes, and rebinds its note subscription; it follows the evidence store's `repositoryId`, as `relations-store.ts` does. — A render must never show one codebase's notes against another's findings. — None. |
| IP38 | **Ruling:** a note whose `status` is missing or not a string shows "No status"; nothing writes a status except creation (`open`). — O6: the status is the user's. — None. |
| IP39 | **Ruling (implements IN13):** preview reads happen only in the store's `readPreview`, triggered by a selection change or Reload; each read takes a token and a result for an older token is dropped. Re-imports and list renders never read. — IN11 and IN13. — None. |
| IP40 | **Ruling:** new copy uses the prefixes `INVESTIGATE_`, `PREVIEW_`, `NOTE_`, `NOTES_`, `REFRESH_`, `UNCERTAINTY_`, `CHECKLIST_`, `CITY_FINDINGS_`; the existing `INVESTIGATE_HOTSPOT_TITLE`, `INVESTIGATE_HOTSPOT_DETAIL`, `INVESTIGATE_MODULE_DETAIL`, `INVESTIGATE_FILE_TITLE`, `INVESTIGATE_LARGEST_DETAIL` and `INVESTIGATE_FILE_LABEL` are untouched. — `export *` re-exports would shadow a duplicate name silently or fail the typecheck. — None. |
| IP41 | **Ruling:** the plan adds five modules that spec §3 does not list: `src/ui/read-models/investigation-evidence.ts` (the bundle, uncertainties, checklist, note facts, preview request and refresh changes, split from `investigation.ts` before either nears 400 lines), `src/application/investigation/root-path.ts` (root joins and containment shared by the preview and the host), `src/host/investigation-services.ts` (builds the notes port and the preview once in `onload`, inert until used, so `main.ts` gains three lines), and the fixtures `tests/fixtures/fake-investigation.ts` (an inert notes port and a scripted preview, for `dataPortDeps` and the component tests) and `tests/fixtures/fake-investigation-folders.ts`. — §3 names the layers and the main files, not every helper; each addition keeps a named file under its cap or removes a duplicate. — None. |
| IP42 | **Ruling:** the `investigations` store is **not** an application port. Its interface, `InvestigationFolderStore` (`read`, `write`, `purge`), is declared in `src/adapters/storage/plugin-data-investigation-store.ts`, with its slice decode and apply rules module-private there; only the host (`settings-tab.ts`, `investigation-notes.ts`, `investigation-services.ts`) uses it. — Spec §3 says "one application port", and the host already takes adapter types directly (`ReviewRepositoryRegistry` in `settings-tab.ts`). Keeping the rules module-private also keeps them from being dead exports; they are tested through the store over the mock `Plugin`. — Low: a later UI need for the folder goes through the notes port's `destination`, which already exists. |
| IP43 | **Ruling:** the preview's defensive state for a missing filesystem adapter is named `no-filesystem` (not `mobile`), with the words "Source preview needs the desktop app’s file access." — Spec §3 and IN9 call it "no filesystem", and the plugin is `isDesktopOnly`, so "mobile" would name a case that cannot happen. — None. |
| IP44 | **Ruling:** the IN39 injection test reaches the specifier and a zone name by parsing `syntheticFallowJson`'s output, setting `check.unresolved_imports[0].specifier` and `check.boundary_violations[0].from_zone` to the hostile string, and re-serialising before the real parser reads it; the symbol goes through the existing `symbol` option and the file path through `snapshotWithPaths`. — IN39 names four carriers, and the synthetic generator exposes only the symbol; editing its JSON keeps the real parser in the path (no parser mocks). — None. |
| IP45 | **Ruling (O8):** the host-independent native code's unit tests live in the fast suite's `tests/unit/` — `tests/unit/session-lifecycle.test.ts` and `tests/unit/native-results-gate.test.ts` — not in describe's `tests/native/`. — `vitest.config.ts` collects only `tests/{unit,contracts,integration,host,build}/**/*.test.ts` (node) and `tests/{component,acceptance,benchmarks,harness}/**` (jsdom), so a `tests/native/` file would never run; `tests/unit/` needs no config change and joins the existing G8 `unit` row. — None. |
| IP46 | **Ruling:** the gate's required scenarios are one file, `tests/e2e/required-scenarios.json`, read by `scripts/check-native-results.mjs` (through its own `import.meta.url`, so the gate's unit test can run it from a temp directory) and by `tests/unit/native-results-gate.test.ts`. Task 0 lists eight titles (the smoke, the five Obsidian facts, the two cleanup tests); Task 11 adds "renders the Investigate route in a real leaf"; Task 16 adds "keeps human sections byte-identical across a real refresh and links the note through the metadata cache". — describe kept the list twice (script and test), which can drift; each task that adds a native scenario must also make it required, or the gate would pass without it. — None. |
| IP47 | **Ruling (IN49, controller):** the default app version is the documented constant `NATIVE_BASELINE_VERSION = '1.13.4'`, the earliest public release that satisfies `manifest.json`'s `minAppVersion` 1.13.0; a unit test (`tests/unit/native-baseline.test.ts`) pins it at or above `minAppVersion`. The installer version is `latest`, as in describe, and both resolved versions are recorded per case. If 1.13.4 cannot be downloaded, the run fails and Task 0 stops (Step 1); no other version is tried. — 1.13.0 was never published (`obsidianmd/obsidian-releases` lists v1.13.4, v1.13.6, v1.13.7, v1.13.8 on 2026-09-25), so defaulting to `minAppVersion` would fail every run. — Low: the native suite does not exercise 1.13.0–1.13.3 (never public); raising `minAppVersion` to 1.13.4 is left to the owner. |
| IP48 | **Ruling:** the smoke reaches the city view through the plugin's own command, `executeObsidianCommand('codebase-inspector:open-city')`, and finds it as `.workspace-leaf-content[data-type="codebase-inspector-city"] .codebase-inspector-root` (the view type from `city-view.ts:50`), asserting `.ci-screen--overview` (the default route) and one leaf of that type. "Without errors" is proved by hooking `console.error`, window `error` and `unhandledrejection` while the plugin is disabled, then enabling it; any captured text fails the test, and an unrelated Obsidian message in a fresh vault is reported for a named exception, never filtered broadly. — The plugin opens nothing on load (a user directive in `main.ts`), so the smoke must open it; describe's own smoke disabled and re-enabled its plugin the same way. — Low: a noisy Obsidian version needs one ruling. |
| IP49 | **Ruling (IN43):** `tests/e2e/tsconfig.json` extends the root, sets `lib` to ES2022 (for `AggregateError`) and `types` to `node`, `webdriverio` and `wdio-obsidian-service`, and includes `tests/e2e/**/*.{ts,mts}` and `tests/support/**/*.ts`; `tsconfig.test.json` excludes `tests/e2e/**` (its `vitest/globals` and mock-facing types must not meet WDIO's) and adds `ES2021.Promise` to its `lib` (it typechecks `tests/support/session-lifecycle.ts` through the unit test). `npm run typecheck` gains the third project, so `verify` typechecks the native files without running them. `src` stays ES2020 through `tsconfig.json`. — Our root `lib` is ES2020, which has no `AggregateError`, and describe had no fast-suite `obsidian` alias to keep apart. — Low: a test in `tests/unit/` may use `Promise.any`, which `src` still cannot. |
| IP50 | **Ruling:** lint for the native layer: `'./tests/e2e/tsconfig.json'` joins `parserOptions.project`; `.obsidian-cache/**` and `reports/**` join the eslint `ignores` and the oxlint `ignorePatterns`; the two `tests/**/*.ts` globs (max-lines 450, `hardcoded-config-path` off) widen to `tests/**/*.{ts,mts}` so the `.mts` config is covered; and one block scoped to `tests/e2e/**` turns off only those of `obsidianmd/prefer-create-el`, `no-tfile-tfolder-cast`, `no-global-this`, `no-unsupported-api` and `prefer-active-doc` that actually fire (describe's list), each named in the Task 0 report. No `max-lines` exception. — Our eslint uses `parserOptions.project`, not describe's `projectService`, so an unlisted tsconfig fails type-aware linting; the native files drive the host from a Node runner, which those plugin-source rules do not describe. — Low: a real hazard in test code under a disabled rule is caught only by review. |
| IP51 | **Ruling:** no automated accessibility (axe) native test and no mobile emulation are ported: `@axe-core/webdriverio` is not added, and `OBSIDIAN_UI=mobile-emulation` is dropped. — The plugin is `isDesktopOnly`; describe's axe scan certified one modal of its own, and this part's accessibility is covered by the component tests and the manual matrix. Fewer dependencies, fewer pins. — Low: native accessibility stays manual; a later part can add axe with describe's `setLegacyMode()` fix. |
| IP52 | **Ruling:** the escaping probe (fact b) renders two literals kept in `tests/support/probe-strings.ts` — `PROBE_HOSTILE` as a positive control that must produce every link, embed, tag, highlight, math span, external link and HTML element (and hide the `%%` comment), and `PROBE_ESCAPED` that must produce none — in reading view (`MarkdownRenderer.render`) and in live preview. Task 2 then pins `noteText(PROBE_HOSTILE) === PROBE_ESCAPED`, so the native proof applies to our real function. — Task 0 runs before `noteText` exists, and a probe without a positive control passes when the selectors are wrong. — Low: the live-preview token class names are version-specific; the control records the ones found, and a class rename shows as a failed control, not a silent pass. |
| IP53 | **Ruling:** native cases are **not** a G8 layer. `tests/unit/gate-evidence.test.ts` counts `*.test.ts`/`*.steps.ts` files per `tests/` directory, so `tests/e2e/` (`*.e2e.ts`) and `tests/support/` (no tests) never appear there; Task 18 records the native run in its own evidence subsection (command, gate line, versions, platform, commit). The two new `tests/unit/` files are counted in the `unit` row at Task 18's single count refresh (L28). — Native runs are opt-in, local and not part of `verify`; putting them in G8's table would state counts nobody reran. — None. |
| IP54 | **Ruling:** `scripts/native-tests.mjs` keeps describe's order (build, run, gate) but runs our `npm run build` (vite + manifest copy + bundle assert) with `shell: true` only on Windows (Node 24 refuses to spawn `.cmd` files without it), runs Vitest as `process.execPath node_modules/vitest/vitest.mjs` (no shell, no `npx`), **always** runs the gate after the run, and exits non-zero when either failed. `SOURCE_COMMIT` falls back to `'local'` (describe's `GITHUB_SHA` fallback is dropped: no CI). — describe's `build.mjs` and `process.mjs` do not exist here, and its runner only ever ran on Linux. — Low: the gate runs against a stale report when the run crashed before writing one; it then fails on a missing file, which is the intended outcome. |
| IP55 | **Ruling (IN51 d):** the native spine drives the real UI end to end — the scan through the plugin's own source and scope modals (vault-folder mode on a `code/` folder copied into the session's vault), the report through the Data & scans import dialog's file input, creation and refresh through the Investigate dialogs — and checks the link through Obsidian's own `metadataCache`, not through the notes port. — The port is built inside `onload` and is not reachable from `app` without adding a test-only hook to `src`; the UI path also proves the wiring the in-memory spine cannot. — Medium: a long selector-driven flow is the most fragile native test; a failure is reproduced with the recorded versions before any selector changes (IN46). |
| IP56 | **Ruling:** the fixture vault `tests/e2e/vault/` holds Markdown only (`Welcome.md`), never a configuration folder, and native files never import `tests/fixtures/**` or `tests/mocks/**` (so the fast suite's `obsidian` alias and jsdom helpers cannot leak in); the native spine copies `tests/fixtures/fallow/relations-project/` into the **session's copied vault** at run time with `node:fs` and hashes it with a local helper. — Our `.gitignore`'s `.obsidian` entry would silently drop a committed config folder, and `copy: true` means the committed vault is never the one written to. — None. |
| IP57 | **Ruling:** exact pins for the native stack: `wdio-obsidian-service` 3.2.1 and `webdriverio` 9.32.0 as devDependencies, and `"overrides": { "@puppeteer/browsers": "3.2.3" }`; the service's optional peers (`appium`, `appium-uiautomator2-driver`) stay uninstalled; no Mocha, no WDIO runner. — The lifecycle hooks used (`launcher`, `ObsidianWorkerService`) are describe's version-pinned seam, and the override keeps describe's fix for the vulnerable `extract-zip`. — Low: upgrading either package is its own change with its own native run. |
| IP58 | **Ruling (IN50):** Task 0 Step 1 is a go/no-go on this Windows 11 machine before any other native file is written: install the pins, build, and start one real session. If it cannot start, Task 0 stops with nothing committed, the controller records the failure as an IPF ruling, and — per the owner's fallback — the plan continues without the native layer: the five §6 probes are run by hand, and Tasks 11, 16 and 18 skip and record their native steps. — describe never ran native tests on Windows, and a half-committed harness that cannot run would only add red. — Medium: the owner's easy e2e testing waits for a later fix on this machine. |

## Pre-flight rulings

The pre-flight scan (2026-09-25, at bee136b) re-measured every Global Constraints line count with `wc -l`, grepped every "Consumes" name, path and line reference, and checked one row per pair of tasks sharing a file or interface and one row per task for internal consistency. It also confirmed Task 0's inputs: `codebase-inspector:open-city` and the view type `codebase-inspector-city` exist, `npm run build` writes `dist/manifest.json`, `npm run analyze` reports exactly 9, and the three pinned native packages exist on npm. It found 0 blocking, 12 rework and 14 cosmetic issues. Three line counts were stale (`use-read-models.ts` 178, `ArchitectureScreen.vue` 256, `tests/harness/mount.ts` 389); none breaks a cap. Task 0 Step 1's go/no-go (IP58) and one ruling per Obsidian fact (a)–(e) from `tests/e2e/obsidian-facts.e2e.ts` (IN51 b) are recorded after Task 0 runs, as IPF14 onward.

| # | Ruling |
|---|---|
| IPF1 | **Ruling:** each creating task (2, 3, 4, 5, 9, 10) keeps constants and types that only their own module or only tests use module-private from the start (`NOTE_VALUE_MAX`, `NOTE_NAME_MAX`, `COLLISION_MAX`, `sanitizeNoteName`, `EVIDENCE_BLOCK_MAX_BYTES`, `EVIDENCE_LIST_MAX`, `portableFingerprint`, `PREVIEW_MAX_BYTES`, `PREVIEW_CONTEXT`, `PREVIEW_LINE_MAX`, and the helper types the scan lists); tests use literals and reach `sanitizeNoteName` through `noteBaseName` and `validateNoteName`; the pin `expect(COLLISION_MAX).toBe(99)` is dropped. Tasks 6 and 10 run `npm run analyze` against a named list of expected transient findings (every Task 2–5 export and file whose consumer task the plan names); only a finding outside it is removed. Tasks 15 and 18 must read exactly 9. — Global Constraints "Dead exports", spec §7; `LOT_FOOTPRINT` in today's baseline shows local use does not count. — Low: churn if an implementer privatises a planned export, or drift unnoticed until Task 18. |
| IPF2 | **Ruling:** Task 15 edits `tests/component/file-detail-screen.test.ts:49` in place to the new `FILE_SOURCE_PREVIEW_LATER` words. — IN41/IP32 reword it; the same in-place rule as Task 8's `settings-fallow.test.ts:179`. — None. |
| IPF3 | **Ruling:** Task 0 gains a step that writes `tests/unit/native-baseline.test.ts` (RED by temporarily setting the baseline to `1.12.9`) and adds it to the gate and the commit; its Produces reads `OBSIDIAN_VERSION ?? NATIVE_BASELINE_VERSION` with `NATIVE_BASELINE_VERSION = '1.13.4'`; the unused `readFileSync` import in `session.ts` is dropped (a named deviation from describe). — IN49, IP47. — Low: the baseline pin would silently not exist. |
| IPF4 | **Ruling:** if Task 0 stops under IN50, Task 2 creates `tests/support/probe-strings.ts` with Task 0's two literals exactly as Task 0's Produces gives them. — IP52/IP58 keep the probes. — None. |
| IPF5 | **Ruling:** Task 11's native smoke proves RED with a label that is not a substring of any nav item (e.g. `'Investigation'`), not `'Investigat'`. — WDIO's `*=` is a contains match; the RED rule and IN46. — Low. |
| IPF6 | **Ruling:** each `.ci-source-preview__line` span ends with a literal `\n` inside the span; no `display: block`; no text nodes between tags. Task 12's test splits `textContent` on `\n` as written. — IN8; copy-paste and screen readers get real line breaks. — Low. |
| IPF7 | **Ruling:** Task 11's entry-point reset also pages: when the selected row's index `i` is at or beyond `shown`, `shown` becomes the smallest multiple of `FINDINGS_PAGE` above `i`; one case pins 150 findings with an entry to row 130. — IN4 ("the row is listed"), IP19. — Low. |
| IPF8 | **Ruling:** `freeNoteName` cuts the base to `NOTE_NAME_MAX − ' (99)'.length` code points (re-stripping trailing dots and spaces) before adding a suffix, so every candidate is at most 100 code points; tests in Task 2 (a taken 100-point base) and Task 9 (the create succeeds as `(2)`). — IN21 and IN27. — Low. |
| IPF9 | **Ruling:** `CreateNoteDialog` and `RefreshNoteDialog` use `src/ui/kit/use-busy-action.ts` (`run`, `requestClose`, `busy`), not a hand-rolled scaffold. — Part 5 E19 and E13; the verbatim-duplication rule. — None. |
| IPF10 | **Ruling:** Tasks 2, 3, 4, 5, 9 and 10 run the three whole-`src` scans (`clean-vault-install`, `no-process-execution`, `node-access-boundary`) with their timeouts; Task 8's settings row uses `name: NOTES_FOLDER_SETTING_NAME` and runs `clean-vault-install.test.ts`; Task 18 notes the checkpoint #4 settings list is unchanged because the name is a constant. — Global Constraints "Whole-src scans". — Low. |
| IPF11 | **Ruling:** `tests/fixtures/fake-investigation-folders.ts` (Task 8) is harness-safe (no `vitest`, no `node:*`) and joins the Global Constraints list; Task 17 uses `createFakeProfileStoreHarness` and `createFixedClock` and adds a harness-safe `fixedSourcePreview(result)` to `tests/fixtures/fake-investigation.ts`. — IP30, IP36, the WP-03 Task 14 lesson. — Low. |
| IPF12 | **Ruling:** Task 13's Produces names the Create investigation note button's class `.ci-notes-panel__create`, which Tasks 16 and 17 click. — IN26, IN40. — None. |
| IPF13 | **Ruling:** the 14 cosmetic findings (PF-C1 to PF-C14: stale line references, prefix lists, incomplete Consumes lists, merging Task 10's watcher into Task 1's, delegating `PREVIEW_STALE_LOCATION` to `UNCERTAINTY_LINE_STALE`, measuring `InvestigateScreen.vue` and `audit-copy/investigation.ts` at Tasks 13 and 15, a code-point refine on the zod folder bound, labelling probe (c)'s 5 s wait as an event-or-timeout record, and checking describe's verbatim files and the service's exports right after the install) are carried as notes in each task's dispatch. — Each is a fact or wording fix. — None. |
| IPF14 | **Ruling (go/no-go, IP58):** GO. Task 0 Step 1 started a real Obsidian session on this Windows 11 machine: app 1.13.4 and installer 1.13.4 on win32, downloaded into `.obsidian-cache/` (513 MB). The whole native suite then passed 8/8 in one run (35ca8d9). — IN50's stop rule did not trigger, so the in-memory fallback is not needed and the native scenarios stay. — Low: other Windows machines or Obsidian versions are untested. |
| IPF15 | **Ruling (fact (b), probe correction):** both probe (b) literals use the host `x.io`. The first run's positive control failed because Obsidian does not autolink `https://x` or `www.x` (a host needs a dot); with `x.io` the control shows 2 external links in reading view while the escaped `https\://x.io` and `www\.x.io` stay inert. Task 2's `noteText` pin consumes the new literals from `tests/support/probe-strings.ts`. — A positive control that cannot fire proves nothing (IN46); the escaping was never at fault. — None. |
| IPF16 | **Ruling (fact (a)):** `vault.create` rejects an existing path ("File already exists.") and leaves the file's text unchanged, and it also rejects a case-only variant (`PROBE-A.md` against `probe-a.md`) on this Windows vault. IN27's pre-check plus the case-insensitive sibling check (IP9) stay; the rejection is the race guard it was planned as. — Observed in real Obsidian 1.13.4. — Low: a case-sensitive Linux vault behaves differently, which IP9 already covers. |
| IPF17 | **Ruling (fact (b)):** backslash escapes suppress wikilinks, embeds, tags, `%%` comments, highlights, math and auto-links in both reading view and live preview (0 tokens or elements for the escaped text; every category present for the control). IN24's escaping plus code spans stands as designed. — Observed in real Obsidian 1.13.4. — Low: a future Obsidian parser change is caught by this probe, which stays as a regression test. |
| IPF18 | **Ruling (fact (c)):** `metadataCache` fires `changed` after `vault.create`, `vault.process` and `processFrontMatter`. IN33's incremental index is primary; the `resolved` rebuild stays as the repair path. — Observed; no event timed out. — Low. |
| IPF19 | **Ruling (fact (d)):** `stringifyYaml` round-trips every probe value as a string: `yes` is written unquoted and still reads back as the string `yes`; `null`, `0012`, `a: b`, `true`, `~`, `#x` and `[[x]]` are quoted. IN20's string-typed frontmatter and IP13's string validation on read stay as designed. — Observed. — Low: a hand-edited note can still hold non-strings, which IP13 counts as malformed. |
| IPF20 | **Ruling (fact (e) and the locale):** `vault.createFolder('p1/q1')` creates the missing parent. IN28 still creates one segment at a time, because IP9 matches existing segments case-insensitively and uses their case. Separately, the session UI follows the system locale (German on this machine), so native tests never match Obsidian's own UI text; they use selectors, command ids and `executeObsidian`. — Observed. — Low. |

## Execution rulings

| # | Ruling |
|---|---|
| WP-04 E1 | **Ruling:** the two `// oxlint-disable-next-line preserve-caught-error` lines in `tests/support/session-lifecycle.ts` (ported from describe) are ratified, with a comment. — `AggregateError` has no `ErrorOptions` without the ES2022 lib (IP49); behaviour-neutral. — None. |
| WP-04 E2 | **Ruling:** probe (b)'s literals end a paragraph with a block id (` ^probe-id` / ` \^probe-id`), and the probe asserts the control's id is registered and the escaped one's is not. — Spec §6(b) names block ids, and the mid-paragraph `^block` is never one, so there was no positive control. — None. |
| WP-04 E3 | **Ruling:** the Investigate screen's gone announcement uses `reannounce(live, message)`, not the brief's `computed`. — The Global Constraints' announcement rule outranks the brief's code sample. — None. |
| WP-04 E4 | **Ruling:** `noteText` escapes the `.` of every `www.`, case-insensitive, wherever it occurs. — `\b(www)\.` let `_www.x` through (`_` is a word character). — Low: `\.` shows in source view. |
| WP-04 E5 | **Ruling:** `npm run analyze` above 9 is accepted between tasks when every extra finding is an export whose consumer is a named later task (IPF1); it returned to 9 at Task 9, again at Task 12, and is 9 at Task 15 and after. — The plan builds pure modules before their consumers. — None. |
| WP-04 E6 | **Ruling:** `NOTE_TYPE` stays exported for Task 5 (a controller omission in the Task 3 dispatch's transient list). — Task 5 consumes it; privatising it would duplicate the literal. — None. |
| WP-04 E7 | **Ruling:** `joinRootPath`/`relativeInside` transients are expected until Task 9. — Task 9 consumes them. — None. |
| WP-04 E8 | **Ruling:** one shared invisible/bidi set (`INVISIBLE_CONTROLS`, built numerically) is exported from `note-text.ts` and used by the note text and the source preview. — The two copies had drifted into literal characters. — None. |
| WP-04 E9 | **Ruling:** implementers build invisible and bidi code points numerically and scan touched files before committing. — Typed `\u` escapes landed as raw characters twice (Tasks 2 and 4), the Trojan Source pattern in the feature that defends against it. — None. |
| WP-04 E10 | **Ruling:** the stale-report uncertainty is worded by `staleCauseOf(report)`: the older-snapshot words, or `UNCERTAINTY_REPORT_FAILED_RUN` for a report stale after a failed run. — The single sentence was false after a failed run, and it is written into notes as fact. — Low: wording. |
| WP-04 E11 | **Ruling:** Quality's Severity options keep their existing `localeCompare` order. — Existing Quality behaviour, outside this part. — Low: two unlisted severity words may sort differently from the rows' code-unit order. |
| WP-04 E12 | **Ruling:** the fake vault fires the metadata `changed` event asynchronously, after the write resolves. — Real Obsidian fires it after re-parsing (the native probe had to wait for it); a synchronous fake would let tests pass that fail in the app. — None. |
| WP-04 E13 | **Ruling:** Task 8's report stands on line endings (git blobs are LF; CRLF only in the checkout), and the code-point bound's RED was shown with a UTF-16-unit mutation because zod 4.6.5's `.max()` already counts code points. — Both are facts checked by the controller. — None. |
| WP-04 E14 | **Ruling:** the note index rebuilds from `getMarkdownFiles()` only on the first `resolved` after it starts; afterwards only `changed`, `rename` and `delete` update it. — `resolved` fires after every modification (obsidian.d.ts:4468), so a rebuild per event undid IP13; IPF18 confirmed the incremental events fire. — Low: a missed event is not repaired until reload. |
| WP-04 E15 | **Ruling:** a refresh whose block write succeeded but whose frontmatter update failed returns `'partial'`, with copy that says the evidence was refreshed and the note's fields were not. — `write-failed` would claim nothing changed (E17 of Part 2). — None. |
| WP-04 E16 | **Ruling (spec note):** a refresh updates `snapshot_id` and `source_path` only; `entity_id` and `finding_fingerprint` keep the identity the note was created for (IN32 as written). — A note stays linked to the finding it was written for. — Low: after a source path change, `entity_id` names the old path. |
| WP-04 E17 | **Ruling:** `'partial'` is a plain string in the `RefreshNoteResult` union (correcting the controller's own E15 wording, which asked for an object). — The union is otherwise string literals. — None. |
| WP-04 E18 | **Ruling:** focus after Show more reveals the last page is not moved (deferred). — Quality has the same pattern; out of this part. — Low: keyboard users land on the page body. |
| WP-04 E19 | **Ruling:** a ready preview records the line it was read for; the verdict is null ("not checked") when the finding's current line differs, and the highlight and its label require the line to be in the window. — A re-run that moves the finding keeps its fingerprint, and the old window would otherwise earn an exact highlight on a line not shown. — None. |
| WP-04 E20 | **Ruling:** remounting the Investigate screen never re-reads when the store holds a ready or loading preview for the same fingerprint. — IN13, one read per selection. — None. |
| WP-04 E21 | **Ruling:** the "created and excluded" announcement names the root-relative exclusion actually saved. — The user finds that value in Excluded paths. — Low: wording. |
| WP-04 E22 | **Ruling:** a create outcome is announced only while the same codebase is still bound. — After a switch, "this codebase's excluded paths" would describe another codebase. — Low: a create finished after a switch is silent (the note is listed on return). |
| WP-04 E23 | **Ruling:** the refresh dialog resolves its note from the current index each time; a note re-linked to another finding is refused as `not-linked` without calling the port. — The host check does not carry the fingerprint, so a stale link would refresh a note with the wrong finding's evidence. — None. |
| WP-04 E24 | **Ruling:** a refresh outcome that resolves after its dialog closed is announced (success or refusal words), same codebase only. — The user must always be told what happened to their note. — None. |
| WP-04 E25 | **Ruling (amends IP14):** a profile with no LocalBinding (as `scan-codebase` creates one, by WP-01's documented open decision) previews under the in-memory snapshot's own `scope.rootPath`; a bound profile must still equal the snapshot's root, else `no-binding`; no snapshot never reads. — Found by the native spine: in the real app the preview otherwise never worked in the default flow. The snapshot store is in-memory only, so the root is always one this session's approved scan read. — Low: a preview reads under a root the user approved for scanning but never "connected" in Settings. |
| WP-04 E26 | **Ruling:** the note's evidence-state words are Current / Stale, not Collected / Stale. — "Collected" read as the collected-report origin next to "Imported report". — Low: wording. |
| WP-04 E27 | **Ruling:** two UI defects seen in the harness captures (no inner padding in the create and refresh dialogs; an always-rendered empty chips paragraph leaving a gap in the Evidence panel) are fixed in Task 17's fix round with re-shot captures. — They were earlier tasks' CSS/template; the harness task could re-shoot. — None. |

Also recorded during execution:
- **Full suite after Task 15** (the last task that touches `src` before the E25/E27 fixes): 299/301 files, 3414 passed, 1 skipped; the only failures were the two evidence-count tests Task 18 refreshes by design. No cross-task regression.
- **Network errors:** the Task 6 reviewer was cut off once and resumed.
- **Open items found by the native spine** (outside this part): the Settings tab does not refresh after `scan-codebase` creates a profile (pre-existing); the native test vault's watcher did not index files copied in with `node:fs` (test environment).

## Deferred minors

Each is to be triaged by the final whole-branch review.
- **Task 2:** `LPT0` and `CONOUT$` are not tested separately (the same character class as `COM0` and `CONIN$` is).
- **Task 3:** the evidence block's hard size guard is unreachable through the shrink ladder (its last step is already bounded), so it is untested, and it is not escape-aware if ever reached.
- **Task 4:** the one retry read after a stat/read race is not itself verified.
- **Task 10:** the destination token is bumped twice per bind (harmless).
- **Task 11:** no test that a filter change resets paging after it grew past one page.
- **Task 12:** no dedicated idle-state test; the no-line case does not assert the uncertainty panel.
- **Task 16:** a stale comment in `audit-copy/investigation.ts` (~92-93) still says "collected or stale".
