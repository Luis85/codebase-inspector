---
project: codebase-inspector
title: WP-04 Part 2 — SDD ledger (rulings)
date: 2026-09-26
branch: feat/wp-04-part2
---

# WP-04 Part 2 — rulings

This ledger records every ruling made while planning and executing WP-04 Part 2 (native e2e coverage), and what each one costs if it is wrong.

- **Spec:** `docs/superpowers/specs/2026-09-26-wp04-part2-native-e2e-design.md`. It holds decisions NE1–NE20, owner decisions O1–O4, 26 scenarios and probes a–k.
- **Plan:** `docs/superpowers/plans/2026-09-26-wp04-part2-native-e2e.md`, 12 tasks.
- **Branch:** `feat/wp-04-part2`, from `353e2c1`, the PR 1 head after WP-04 Part 1.
- **Precedent (all binding):** the WP-04 Part 1 ledger (O8, IP45–IP58, IPF14–IPF20, E12, E14, E25), and every earlier ledger.
- **Numbering:**
  - **O1…** are owner decisions;
  - **NE1…** are the spec's decisions;
  - **NP1…** are planning rulings;
  - **NPF1…** are pre-flight rulings;
  - **"WP-04.2 E1…"** are execution rulings.
- Every ruling reads: **Ruling:** what — why — cost if wrong.

## Owner decisions (2026-09-26, before the spec)

| # | Decision |
|---|---|
| O1 | **All candidates** the audit confirms. That means:<ul><li>lifecycle and leaks;</li><li>persistence across a reload;</li><li>all six commands by id and the ribbon;</li><li>the Settings tab;</li><li>import with a real file;</li><li>the E25 bound preview;</li><li>the city in dark and light, and a layout restore after an app restart;</li><li>the note index and the E14 reload repair;</li><li>refresh `markers-edited` and `partial`;</li><li>the O7 in-root exclusion;</li><li>the four Part 1 follow-ups.</li></ul> |
| O2 | **Fix exposed bugs here, RED first**, each with a WP-04.2 E ruling and its cost. The Settings-refresh bug is fixed. |
| O3 | **No CI.** The native suite stays local and opt-in; CI is a follow-up. |
| O4 | **Baseline gate (1.13.4) plus one latest run** at the finish, which must pass every required scenario and records its resolved version. |

## Planning rulings

| # | Ruling |
|---|---|
| NP1 | **Ruling:** 12 tasks in this order:<ol><li>probes and helpers;</li><li>Settings with the NE9 fix;</li><li>lifecycle;</li><li>commands;</li><li>fallow;</li><li>note index;</li><li>refresh;</li><li>in-root and alias (NE15 if RED);</li><li>preview;</li><li>city;</li><li>code spans and email (NE16 if RED);</li><li>evidence.</li></ol> — Task 1's probes decide what every later scenario can observe; NE9 lands before Task 5 needs it; `src` may change only in Tasks 2, 8 and 11, so the full suite runs after Task 11. — Low: a mis-ordered dependency shows in the controller's Consumes re-check. |
| NP2 | **Ruling:** scenarios live in ten files by concern:<ul><li>`plugin-lifecycle`</li><li>`city`</li><li>`commands`</li><li>`fallow`</li><li>`settings`</li><li>`notes-index`</li><li>`notes-refresh`</li><li>`notes-root`</li><li>`preview`</li><li>`obsidian-facts`</li></ul>Shared UI steps go in `tests/e2e/inspector.ts`. Host probes go in `host-probes.ts`, run-time fixtures in `workspace-files.ts`, and the PNG reader in `png.ts`. If `inspector.ts` passes 400 lines, it splits into `inspector-<concern>.ts` modules re-exported by `createInspectorPage`. — The brief asks for a split by concern and shared steps in `inspector.ts`. Probes over Obsidian internals and Node-side fixture writers are different kinds of code from UI steps, and each file stays under the 450 cap. — None. |
| NP3 | **Ruling:** a native test may match the plugin's own words only through a constant imported from `src/ui/audit-copy/**` or `src/ui/inspector-copy.ts`, never through a literal. It never matches Obsidian's words (IPF20). The ribbon's `aria-label` "Open codebase city" is the one exception: it is the plugin's own literal (`main.ts:89`) with no constant, and it is used only as an attribute selector. — The locale is German here, while the plugin's copy is English everywhere, and a constant keeps a reworded string from silently breaking a test. — Low: renaming the ribbon label breaks scenario 5 loudly. |
| NP4 | **Ruling:** while developing, one native file runs as `npm run build` then `node node_modules/vitest/vitest.mjs run --config tests/e2e/vitest.config.mts tests/e2e/<file>.e2e.ts [-t "<title>"]`. Acceptance is always `npm run test:e2e`, which builds, runs everything and gates. — Rebuilding and running 36 sessions per edit would make RED proofs impractical; the gate still sees everything at the end. — None. |
| NP5 | **Ruling:** a test that restarts Obsidian or runs fallow passes `300_000` as its own timeout; a synthetic-tree scan passes `240_000`; everything else keeps the config's `120_000`. — The Global Constraints require explicit timeouts on slow work, and a restart alone costs ~15 s plus the service's 2 s pause. — Low: a slower machine needs a larger number, set by ruling, never a retry. |
| NP6 | **Ruling:** restarts use `browser.reloadObsidian()` with no arguments. The service then deletes the session without shutting the driver down and reopens the same copied vault and profile (`wdio-obsidian-service` `dist/index.js:3251–3260`). Probe c confirms that `data.json`, `workspace.json` and `localStorage` survive, and that teardown still releases everything. — A new vault or a new profile would make an app restart look like a fresh install and void scenarios 2 and 4. — Medium: if probe c fails, scenario 4 needs a ruling, and scenario 2 falls back to a plugin reload, which it already uses. |
| NP7 | **Ruling:** scenarios 8, 9 and 15 take the fallow binary from `FALLOW_BIN`, else `.fallow-bin/bin-path.txt`. A missing binary **fails** with a message naming both, never skips, and `test:e2e` fetches nothing. — A skip would fail the gate anyway (IN48), and a fetch would add network access to every native run. — Low: a clean checkout must run `npm run test:fallow` once, or set `FALLOW_BIN`. |
| NP8 | **Ruling:** a mid-run cancel is proved against a synthetic tree whose size probe g fixes. The uncancelled control run must take at least 2 000 ms, or the test fails. The cancel is sent only after the command's own `checkCallback(true)` answers true. — A small tree finishes before the cancel arrives: the test would then exercise nothing, or fail at random. The measured control makes a too-short window a visible failure. — Low: a much faster machine needs a bigger tree, by ruling. |
| NP9 | **Ruling:** the canvas pixel is read from WebDriver's element screenshot with a small PNG reader on `node:zlib` (`tests/e2e/png.ts`, 8-bit RGB/RGBA, filters 0–4). — WebGL `readPixels` without `preserveDrawingBuffer` returns cleared buffers, and a PNG library would be a new dependency (spec NE2). — Low: a screenshot format change fails the decoder loudly ("unsupported PNG"). |
| NP10 | **Ruling:** NE9 is a data-write listener in `plugin-data-shape.ts` (`watchPluginData`). Both write paths notify it, `writePluginDataSlice` (only when it actually saved) and `updatePluginDataRecord`, and the tab coalesces with `refreshSoon()`. `ProfileStore` gains no member. — Every writer the tab misses goes through those two functions (a scan's new profile, the exclusion migration, a note's exclusion, a fallow bind or trust). A `ProfileStore` event would change a frozen WP-01 §4.5 port and miss analyzers. — Low: a write that bypasses `plugin-data-shape.ts` would be missed; none exists (outside that module, `saveData`/`loadData` appear in `src/` only in comments). |
| NP11 | **Ruling (NE14, the Part 1 follow-up):** a note the user moved inside the codebase root is refreshed normally. The refresh writes only between its markers, after the dialog's confirm, and adds no exclusion; the next scan lists it as a codebase file. Scenario 21 pins that nothing else under the root changes. — O7's checkbox guards a note the **plugin** places inside the root. Here the user placed it, and refusing would strand a note the user deliberately keeps next to the code. — Low: the owner may prefer a refusal or a warning; that is one dialog line and a copy constant. |
| NP12 | **Ruling:** scenario 24's Markdown anchor comes from a report crafted at run time from the 3.27.0 recording, with one finding's path set to `docs/guide.md`, through the real importer. — fallow never reports Markdown files (IN29), so Open in Obsidian is unreachable through a real report; the importer and the preview are still the real ones. — Low: an importer rule that later rejects such a path turns the scenario RED, and it is reworked then. |
| NP13 | **Ruling:** the pre-flight probes run from a scratch `tests/e2e/probes.e2e.ts` that is deleted before Task 1's commit. Their outcomes, copied from `probe.json`, become NPF rulings. Each scenario that relies on a probe carries that probe's positive control itself. — They are facts about the test environment and Obsidian internals, not plugin behaviour; keeping them as required scenarios would pin internals the plugin never uses. Part 1 kept its facts because the plugin relies on them. — Low: a later Obsidian changing an internal fails the scenario's own control, not silently. |
| NP14 | **Ruling:** the brief's "exclusion confirm" is read as the scope approval the next scan asks for after Excluded paths changes (IN29, M62). Scenario 13 covers it, alongside the saved and the refused edit. — The Settings tab has no exclusion confirm dialog; the confirmation a person meets is the scope modal. — Low: if the owner meant the create dialog's exclusion checkbox, scenario 20 covers that. |
| NP15 | **Ruling (corrects the brief):** "Open in Obsidian" and Open note use `workspace.getLeaf(true).openFile(file)` (`investigation-notes.ts:212`), not `openLinkText`, which `src/` never calls. Scenario 24 asserts a new Markdown leaf on the vault path. — Found by the coverage audit (spec §3). — None. |
| NP16 | **Ruling:** pop-out windows stay out (NE20). — Driving a second Electron window needs a new seam in the harness, and the brief allows no new harness. — Low: `onWindowMigrated` and `activeDocument` in a pop-out remain covered only by `window-migration.test.ts`. |
| NP17 | **Ruling:** every scenario gets its own session (the fixture is per test). 36 sessions make `npm run test:e2e` take several minutes, which is accepted. — IN44 and the brief require a fresh vault copy per test file at least, and a per-test session is what the fixture already gives. — Low: time only. |
| NP18 | **Ruling:** `hashTree`, `cycleFinding` and `RECORDING` move from `investigation.e2e.ts` to `workspace-files.ts` unchanged, and the spine imports them. — Tasks 3, 6, 7 and 8 need them, and a second copy would be a duplication finding in review. — None. |
| NP19 | **Ruling:** scenario 15's RED proof runs against the pre-NE9 versions of `main.ts`, `settings-tab.ts` and `plugin-data-shape.ts`, checked out into the working tree, built, run and restored. If it passes there, the spec §5 mutation is used instead. — The audit says the tab misses analyzer writes, but that reading has not been run yet. — None. |
| NP20 | **Ruling (O4):** the latest run is a second full `npm run test:e2e` with `OBSIDIAN_VERSION=latest` in Task 12. It is not a second gate configuration. Its resolved version is recorded in the gate evidence, and a failure there is a Part 2 failure. — IN49: latest is never reproducible, so it does not belong in the default command. — Low: a new Obsidian release between runs can turn it red; that is reported, never retried. |

## Pre-flight scan

The scan ran on 2026-09-26 at `69914b9`:
- **Line counts:** every Global Constraints line count was re-measured with `wc -l`. All match the plan except `tests/component/settings-tab.test.ts`, which is 437, not 436 (never grown in this part).
- **Consumes names:** grepped and all found:
  - `NOTES_FOLDER_SETTING_NAME` (`audit-copy/investigation.ts:23`), `NOTES_FOLDER_PROBLEM` (`:25`), `REFRESH_PARTIAL` (`:339`), `REFRESH_MARKERS_EDITED` (`:341`);
  - `noteText` (`note-text.ts:39`), `mdCode` (`markdown-code.ts:8`), `writePluginDataSlice` (`plugin-data-shape.ts:68`), `updatePluginDataRecord` (`:92`);
  - the route `city` (`route-ids.ts`);
  - the create-dialog, refresh-dialog and preview classes (`CreateNoteDialog.vue:117–218`, `RefreshNoteDialog.vue:46–103`, `SourcePreviewPanel.vue:95–171`);
  - the machine-id key `codebase-inspector:machine-id` (`plugin-data-binding-store.ts:8`);
  - the recording's `unused_files` entry `src/orphan.ts` (scenario 24's carrier);
  - `node-access.ts` as the one `window.require` file for `fs` (`tests/unit/node-access-boundary.test.ts:98`).

| Row | Tasks | Produces ↔ consumes / self-consistency | Finding |
|---|---|---|---|
| 1 | 1 ↔ 2–11 | Task 1's `host-probes.ts`, `workspace-files.ts`, `png.ts` and `inspector.ts` steps against each consumer's Consumes list | Consistent. Every consumer names only Task 1 Produces members. |
| 2 | 1 ↔ spine | `hashTree`/`cycleFinding`/`RECORDING` moved out of `investigation.e2e.ts` | Consistent (NP18); Task 1 re-runs the spine. |
| 3 | 2 ↔ 5 | Task 2's `watchPluginData` and `refreshSoon` ↔ scenario 15 | Consistent. Scenario 15's RED proof checks out pre-NE9 files (NP19). **Rework (PF-R1):** that checkout must follow a WIP commit, never overwrite uncommitted work. |
| 4 | 2 ↔ 3, 8, 9 | Settings Connect/Reconnect steps (Task 2) ↔ scenarios 2, 22, 23 | Consistent; Task 2 must produce `connect(folder, mode)` and `reconnect(folder)` in `inspector.ts`. **Rework (PF-R2):** Task 2's Produces omits them. |
| 5 | 3 ↔ 6 | `reloadPlugin` (Task 1) used by scenarios 2 and 17 | Consistent. |
| 6 | 4 ↔ 5 | `writeSyntheticTree` size from probe g for both scan and fallow | Consistent. The two sizes may differ; probe g records both. |
| 7 | 7 ↔ 8 | both create notes inside `code/` | No shared file; each test has its own vault. Consistent. |
| 8 | 8 ↔ 11 | `src` edits in different files (`node-access.ts`, `investigation-notes.ts` ↔ `note-text.ts`) | Consistent. |
| 9 | 10 ↔ 1 | probe c (restart) and probe e (pixel) ↔ scenarios 3, 4 | Consistent. |
| 10 | 12 ↔ all | the evidence counts refreshed once | Consistent (L28). |
| T1 | 1 | the scratch file is deleted before commit; the helpers are written from observations | Consistent. |
| T2 | 2 | Step 3 puts a `CodebaseInspectorSettingTab` coalescing case in `tests/unit/` (node project) | **Rework (PF-R3):** the settings-tab tests run in jsdom (`tests/component/settings-tab*.test.ts`). The coalescing case goes in a new `tests/component/settings-tab-refresh-soon.test.ts`, and `tests/unit/plugin-data-watch.test.ts` keeps the watcher cases. |
| T2b | 2 | Step 3 builds `new Plugin({} as never, {} as never)` | Cosmetic (PF-C1): use the mock's constructor as `tests/unit/plugin-data-shape.test.ts` does. |
| T3 | 3 | scenario 1's "enabling opens no tab" after `enablePlugin` | Consistent. |
| T4 | 4 | scenario 7 compares against the recording's normalised count | **Rework (PF-R4):** the count must be the one Investigate lists: Quality's findings over the scanned snapshot (IN1). The implementer derives it with the same `buildEvidenceReport`, and reports if the UI and Node counts differ, instead of adjusting either. |
| T5 | 5 | the fallow binary path | Cosmetic (PF-C2): the dispatch carries the owner's `FALLOW_BIN`. |
| T6 | 6 | scenario 17 moves a note while disabled | Consistent. |
| T7 | 7 | scenario 21's hash comparison "except the note's own entry and its folder" | Consistent. |
| T8 | 8 | the fast alias test skips on non-Windows | Cosmetic (PF-C3): `it.runIf(process.platform === 'win32')`, with the reason in the title. |
| T9 | 9 | scenario 24 edits `unused_files[0].path` (`src/orphan.ts` → `docs/guide.md`) | Consistent: the recording has one `unused_files` entry. |
| T10 | 10 | scenario 3 navigates to the `city` route | Consistent: `city` is a route id. |
| T11 | 11 | an `@` escape vs the IP52 pin | Consistent: `PROBE_HOSTILE` holds no `@`. |
| T12 | 12 | `evidence-numbers.test.ts` is 403 | Consistent, edited in place only. |

0 blocking, 4 rework (PF-R1–R4) and 3 cosmetic (PF-C1–C3) findings.

## Pre-flight rulings

| # | Ruling |
|---|---|
| NPF1 | **Ruling:** PF-R1–R4 and PF-C1–C3 are carried as notes in their tasks' dispatches:<ul><li>(R1) scenario 15's pre-NE9 checkout follows a WIP commit and is restored with `git checkout HEAD -- src`;</li><li>(R2) Task 2 also produces `connect(folder, mode: 'vault-folder' \| 'absolute')` and `reconnect(folder)` in `inspector.ts`;</li><li>(R3) the coalescing case lives in `tests/component/settings-tab-refresh-soon.test.ts`;</li><li>(R4) scenario 7 counts what Investigate lists and reports any disagreement;</li><li>(C1–C3) as the scan says.</li></ul> — Each is a fact or a missing Produces line, not a design change. — Low. |

NPF2–NPF12 are the probe outcomes (a–k), observed in Task 1 (`3e8b6b5`) on Obsidian 1.13.4, win32, each with its positive control passing. NPF13–NPF15 are native facts found later during execution.

| # | Ruling |
|---|---|
| NPF2 | **Ruling (probe a):** `app.workspace`, `app.vault` and `app.metadataCache` keep their listeners in `_` objects of arrays: 23, 7 and 4 event names. One `on('css-change')` adds 1, and `offref` removes it. Scenario 1 compares these tables per event name, and `listenerCounts` throws if a table is missing, so a missing table never reads as zero. — Observed. — Low: an Obsidian rename of `_` fails loudly. |
| NPF3 | **Ruling (probe b):** disabling the plugin detaches every city leaf, leaving one `empty` leaf. Re-enabling restores none, and the leaves are gone after `reloadObsidian()` too. So `onClose` runs at disable. Scenario 1 asserts "enabling opens no tab" at its **first** enable, where no leaf ever existed, never after a disable, where it would be trivially true. Scenario 17 re-opens a city leaf after its re-enable. NE10's leaf observation is recorded, as planned. — Observed. — None. |
| NPF4 | **Ruling (probe c):** `reloadObsidian()` with no argument keeps all of these: the vault path, vault files, `localStorage` (the machine id unchanged), `data.json` (byte-identical) and `workspace.json`. The city leaf survives as its view type. Teardown still completes (`teardown.json` `completed: true`). Scenarios 2 and 4 stand as designed (NP6). — Observed. — None. |
| NPF5 | **Ruling (probe d):** `app.changeTheme('moonstone' \| 'obsidian')` switches `body.theme-light`/`theme-dark` and fires exactly one `css-change` per switch. **Switching to the current theme fires none.** The session starts `theme-dark` (config `system`). Scenario 3 always switches dark → light → dark and never re-applies the current theme. — Observed. — Low. |
| NPF6 | **Ruling (probe e):** the canvas element's WebDriver screenshot holds the renderer's clear colour. At device pixel (2,2), with DPR 2, it equals the resolved `--ci-surface` exactly: dark `[28,28,28]`, light `[255,255,255]`. The match lands within about 450 ms of a switch. Scenario 3 samples (2,2) through `expect.poll`, with the within-3 tolerance (NE13). — Observed; `decodePng` was cross-checked against Chromium's own decoder at 19 points. — Low: a theme or DPR change moves the numbers, never the method. |
| NPF7 | **Ruling (probe f):** `app.setting.open()` + `openTabById('codebase-inspector')` opens the settings in a **separate window**, where `executeObsidian` fails. So:<ul><li>`openPluginSettings` switches WebDriver to that window, and `closeSettings` switches back.</li><li>A settings scenario does every `executeObsidian` step (`pluginData`, commands) outside the settings window.</li><li>A profile's rows are on its own page, reached by clicking its list row (`.setting-group.mod-list .setting-item.mod-navigable`).</li></ul>Probe f also confirmed the NE9 bug: after `scan-codebase`, the tab says it has no profiles. — Observed. — Medium: every settings step must respect the window switch; a missed switch fails loudly, never silently. |
| NPF8 | **Ruling (probe g):** the mid-run window sizes are 2 000 files for `cancel-scan` (the uncancelled scan takes 6.8–7.7 s) and **10 000** files for `cancel-fallow-analysis` (about 11 s; at 2 000 the margin over the 2 s floor is only about 1.2 s). Neither cancel command is available right after the start click (fallow's first ~0.4–0.7 s is its version probe), so each scenario **polls** `commandAvailable` to true before it cancels (NE8). The fallow scenario keeps `300_000`. — Observed. — Low: a much faster machine needs a bigger tree, by ruling. |
| NPF9 | **Ruling (probe h):**<ul><li>The session vault is always reached through an 8.3 path. `getBasePath()` gives `C:\Users\LUISME~1\…`, and `realpathSync.native` gives `C:\Users\LuisMendez\…`.</li><li>The source modal refuses a junction root ("is not a directory that can be read"). That is by design, not a bug: WP-01 §4.4 skips symlinks and never follows them, and a junction root is one.</li><li>Scenario 22 therefore connects the profile in absolute-folder mode to the **long** form of `<vault>/code`, while the vault base path stays in its 8.3 form. That is the alias NE15 must see through, and it is expected to be RED on 353e2c1.</li></ul>— Observed. — Low: a volume without 8.3 names would lack this alias. It exists on this machine's `%TEMP%`, and the test fails loudly if the two forms are equal. |
| NPF10 | **Ruling (probe i):** Obsidian autolinks the bare `a@x.io` in reading view (`href="mailto:a@x.io"`), as it does `<a@x.io>`. Scenario 26 measures whether `noteText` output still links. If it does, NE16's `@` escape applies (O2). — Observed. — Low. |
| NPF11 | **Ruling (probe j):** `processFrontMatter` lives on the `FileManager` prototype. Scenario 19 installs an own-property rejecting replacement for one call and restores the original with `delete`; the next call writes. — Observed. — None. |
| NPF12 | **Ruling (probe k):** `.side-dock-ribbon-action[aria-label="Open codebase city"]` opens a new city leaf per click. Scenario 5 uses it under NP3's named exception (the plugin's own literal, `main.ts:89`, with no copy constant). No `src` change is made to add a constant. — The brief's concern 7; NP3 already rules it. — Low. |
| NPF13 | **Ruling (observed in Task 4):** Obsidian 1.13.4's `app.commands.executeCommandById` returns `true` for any registered command that does not throw, **whatever its `checkCallback` answers**. So the service's `executeObsidianCommand` resolves on a refused command; it rejects only on an unknown id. A refusal is asserted as `commandAvailable(...) === false`, plus the command run by id having no observable effect. It is never asserted as `rejects.toThrow`. — Probed: `cancel-scan` gives `true`, an unknown id gives `false`. The plan's Task 4 and Task 5 wording ("rejects") is corrected by this ruling. — None. |
| NPF14 | **Ruling (observed in Task 6):** a folder move through `app.fileManager.renameFile(folder, …)` fires one vault `rename` for the folder, then one per Markdown file it contains. The note index's per-file assumption (`investigation-note-index.ts` comment) holds on 1.13.4. — Recorded in scenario 16's `folder-move.json`. — None. |
| NPF15 | **Ruling (observed in Task 8; extends the Part 1 open item):** the session vault's watcher never indexes a folder copied in with `node:fs`. `ensureFolders` then calls `createFolder` on a segment that exists on disk but is not indexed, and the create fails as `write-failed`. The scenarios therefore create `code/` through the vault API before copying into it. In a person's vault this can only happen in the short window before the watcher catches up. It is recorded as a limitation, with an optional hardening (`adapter.exists` before `createFolder`) left as a follow-up. — Observed. — Low. |

## Execution rulings

| # | Ruling |
|---|---|
| WP-04.2 E1 | **Ruling:** scenario 13's refused edit is `./dist` together with a `dist*` glob, not `./dist` alone. With `./dist` alone the spec §5 mutation (skipping `exclusionInputReasons`) stayed green: the stored-record validator refuses `./dist` too, with the same words. Only the input-surface rule (M62) refuses a glob, so the glob is what makes the mutation fail. — NE5 requires a RED proof, and a second guard hid the first. — Low: the scenario proves the input rule through its glob refusal, while `./dist`'s refusal is also covered by the stored-record rules. |
| WP-04.2 E2 | **Ruling:** native tests edit a settings text field with click, Ctrl+A, `addValue`, then a click on the page title (blur), never with `setValue`. WebDriver's `setValue` clears the field first, which fires `change` with an empty value and saved `exclusions: []` mid-edit. — Observed in Task 2. A person typing never produces that intermediate `change`. — Low: a later WebDriver change to `addValue` shows as a failing edit, not a silent one. |
| WP-04.2 E3 | **Ruling:** every native step that runs a city-view command first calls `activateCity()`. That covers `scanFolder`, `rescan`, `importReport` and scenario 13's bare `scan-codebase`. `activateCity()` polls until the main window is the active document and the city leaf is the active view (by identity), calling `setActiveLeaf(leaf, { focus: true })` if it is not. The source-modal step also polls for its radio first. — Found by the Task 2 review: `scan-codebase` silently does nothing without an active city view (`commands.ts:46–51`), and nothing waited for focus to return after the settings window closed, which explains the one unreproduced flake. The focus is the precondition a person's own click establishes, never the outcome under test. — Low: a real focus bug in the plugin would be masked by the helper, and none is in scope (commands act on the active view by design, M36). |
| WP-04.2 E4 | **Ruling (amends NE10, and adds a fact to NPF3):** on Obsidian 1.13.4, disabling any plugin that has an open leaf swaps that leaf to Obsidian's unknown-view pane. That pane's `onload` registers a `workspace` `layout-change` listener, which outlives the leaf: +1 per leaf. The core Outline plugin shows the same +1. Scenario 1 therefore measures that per-leaf cost on Outline inside the same test and allows exactly that many `layout-change` listeners per city leaf; every other event name must return to its pre-enable count exactly. — The leak is Obsidian's, not ours: `src/` registers no `layout-change` listener at all (grep). The plugin's own four note-index listeners, both `css-change` listeners, its intervals, roots and canvas are all released. Requiring exact equality would make scenario 1 unpassable on the baseline. — Low: a plugin leak of exactly one `layout-change` per leaf would be masked. `src/` has no such registration, and any new one would push the count past the measured allowance. |
| WP-04.2 E5 | **Ruling:** one run's Vitest worker crashed with 0xC0000409 during scenario 2, and the next three runs were clean. The crash is recorded here, not retried away. If it recurs in any gate run (`npm run test:e2e`, the latest run, or the final verification), that run is a failure and is investigated then. — `retry: 0` stands (IN46), and one unreproduced native crash is evidence, not a defect with a known cause. — Medium: an intermittent Electron or driver crash would fail the final gate. |
| WP-04.2 E6 | **Ruling (extends NP3):** native tests may also take the plugin's nav labels from `ROUTE_META` in `src/ui/routes.ts`, which is the nav labels' own source. The machine-id key `codebase-inspector:machine-id` is read as a literal: it is a storage key, not copy, and its constant is module-private (`plugin-data-binding-store.ts:8`). — Neither is Obsidian's UI text (IPF20), and exporting the key only for a test would add a dead export. — None. |
| WP-04.2 E7 | **Ruling (extends NP3):** a test may also import a plugin-rendered constant from the module that owns it when no copy module exports it, e.g. `CANCELLED_BANNER` from `src/application/run-state.ts` for scenario 6's cancelled banner. `StatusBanner` has no status attribute to select instead. — The words are the plugin's own and the import keeps them in sync; IPF20 only forbids Obsidian's words. — None. |
| WP-04.2 E8 | **Ruling (amends NPF8):** in scenario 6, the NE8 positive control is an uncancelled **refresh** (the same kind of run as the one cancelled), timed at ≥ 2 000 ms. The synthetic tree grows to 4 000 files. The first modal scan only creates the snapshot. — At 2 000 files the refresh that was cancelled took only 1.5–1.8 s uncancelled, under NE8's 2 s floor, while the timed control was the slower modal scan: the window the test relied on was never itself measured. — Low: about 5 s more per run. |
| WP-04.2 E9 | **Ruling:** the Task 4 review's Important point, that the "no observable effect" half of a refusal check (NPF13) was never itself RED-proven, is accepted without a new mutation. The refusal's primary assertion, `commandAvailable` false, is RED-proven (scenario 7's mutation). The command's `checkCallback` runs one shared guard for both checking and executing (`commands.ts:65–87`), so a guard that let the body run would also flip `commandAvailable`. — The reviewer calls it a narrow, disclosed gap, not a check that cannot fail. — Low: an asymmetric guard written in future would be caught only by the no-effect half, which is unproven. |
| WP-04.2 E10 | **Ruling:** scenario 8 runs `run-fallow-analysis` first, then fills in the path, checks, and trusts. The plan's `connectFallow`-first order is not used there (`connectFallow` is still used by scenario 15). Scenario 15's RED proof is the `analyzers` watch mutation: on the pre-NE9 files the scenario fails earlier, because the scanned profile is not listed at all (NP19's fallback). — Check stores nothing, so connecting first then running finds no binding. The pre-NE9 run proves the NE9 bug, not the `analyzers` part, and the mutation fails at the exact executable assertion ("No executable chosen"). — None. |
| WP-04.2 E11 | **Ruling:** scenario 9 requires that no `fallow.exe` is running anywhere on the machine when it starts, and fails loudly if one is. Before the cancelled run it writes one unimported file, so a run that finished anyway would publish a different report and fail the "earlier report kept" check. — The child check has to count processes by image name. The native suite is sequential and never runs beside `test:fallow`. — Low: a stray fallow on the machine fails the gate, and the fix is to stop it, never to retry. |
| WP-04.2 E12 | **Ruling:** WebDriver returns `undefined` as `null`. So `inspector.cachedFingerprint(path)` reads `null` both for a note with no fingerprint and for a missing file. No scenario may treat that `null` as proof of either case. A test that must tell them apart uses a probe returning `{ exists, indexed, hasFingerprint }`, as scenario 16's plain-note check does, with a linked note as its positive control. — Observed in Task 6; the brief's `toBeUndefined()` could never have held. — None. |
| WP-04.2 E13 | **Ruling (NE15, O2):** the alias fix landed after scenario 22 was RED on the unfixed code.<ul><li>The resolver is `realPathOfNearest`: `realpathSync.native` of the deepest existing ancestor, with the rest re-appended, and null on any error. It lives in `node-access.ts` and is injected into the notes port as the optional `realPath` dep, wired in `investigation-services.ts`.</li><li>The overlap takes the textual result whenever there is one, and otherwise compares the resolved paths. `sourceNotePath` shares the same `insideOf` helper and has its own fast test.</li><li>`realPath` stays optional: leaving it out fails safe, back to the textual check, and scenario 22 pins the production wiring.</li></ul>— The textual comparison missed an 8.3 or long-form alias of one folder, so a note could land inside the root with no exclusion offered (O7). — Low: a future refactor that drops the wiring is caught only by native scenario 22. |
| WP-04.2 E14 | **Ruling (follow-up, not fixed):** after Connect or Reconnect to a new root, `scan-codebase` keeps refreshing against the snapshot's stored root with no prompt. No scan reads the binding. So after a Reconnect the preview (which reads the binding) and the snapshot can disagree; scenario 23 relies on exactly that (`no-binding`). The behaviour predates this part (WP-01's refresh against the stored scope), and no Part 2 scenario specifies otherwise. It is recorded as a follow-up for the owner. — O2 covers bugs a scenario exposes. This one was seen while building a flow and needs a product decision on what Reconnect should do to an open snapshot. — Medium: a person who reconnects to another folder keeps scanning the old one until they choose Select a codebase. |
| WP-04.2 E15 | **Ruling (amends spec §5 row 24's RED proof):** scenario 24 stays green under the spec's `getLeaf(false)` mutation. With the city leaf active (a view that is not navigable), Obsidian's `getLeaf(false)` opens a new tab in the same group, exactly like `getLeaf(true)`. The scenario's check that a new tab opened was made strict: a new leaf id, leaf count +1, Markdown leaves +1, the city leaf unchanged, the same tab group. It is RED under two stand-in mutations, `getLeaf('split')` and `getMostRecentLeaf()`. — Observed by a probe in Task 9. A mutation that changes nothing observable from the city leaf cannot be a RED proof. — Low: a future change to `getLeaf(false)` would go unnoticed while the city leaf is active, which is also the only place the button lives. |
| WP-04.2 E16 | **Ruling (amends NP12 and NE19):**<ul><li>Scenario 24's crafted report moves an `unused_exports` path to `docs/guide.md`, because the normaliser makes no finding from `unused_files`.</li><li>Scenario 23's "Reconnect" is Clear binding followed by Connect. A live binding offers no Reconnect button (`setting-definitions.ts`), and that is how a person changes a bound root.</li><li>`recordingFindings` takes a file parameter.</li><li>Scenario 23 scans first, then connects, where NE19 says connect then scan.</li></ul>— Facts of the real importer and settings UI. The reordering is equivalent because no scan reads the binding (E14), and the test asserts that the binding's root equals the snapshot's. — None. |
| WP-04.2 E17 | **Ruling:** scenario 4's "no scan started after the restart" (`commandAvailable('cancel-scan')` false, after `activateCity`) gets no positive counterpart inside the scenario. The Task 10 review raised this as Important. It is accepted on these grounds:<ul><li>The same probe is shown reading `true` mid-run by scenario 6, which is RED-proven under its own mutation and in the same required gate.</li><li>Scenario 4 also asserts the no-snapshot state and no modal, and a started scan would change both.</li><li>A mid-run window inside scenario 4 would need a second synthetic tree and about 30 s more.</li></ul>— The reviewer's own assessment calls it a test-completeness note, since the mechanism is proven elsewhere in the suite. — Low: if scenario 6 were ever removed, this assertion would lose its only positive control. |
| WP-04.2 E18 | **Ruling (NE16, O2):** `noteText` escapes `@` too (`note-text.ts:29`), pinned test-first for both the bare and the angle form (`tests/unit/note-text-email.test.ts`).<ul><li>Scenario 26 was RED before the fix: both escaped forms still autolinked in reading view. That includes `\<a@x.io\>`, where the escaped brackets alone did not stop Obsidian's email autolink.</li><li>The live-preview check uses the `cm-url` token, which the unescaped control shows. Live preview draws no `mailto:` anchor even for the control, so an anchor check could never fail.</li><li>`mdCode` code spans keep `%%`, `$` and `==` inert in both views (scenario 25, no bug).</li></ul>— IN24 promises that no report string can open an auto-link. — Low: a scoped package name such as `@scope/pkg` shows `\@` in source view (reading view renders `@`), like every other escaped character. |
| WP-04.2 E19 | **Ruling:** the Z38 no-freeze timing test (`tests/integration/fallow-analysis.test.ts`, a 50 ms event-loop bound) is not changed in this part. It failed under full-suite load in three runs (62, 57 and 53 ms), while alone and within its own file it passes (19–28 ms). Every other test passes, and `npm run verify`'s typecheck, lints and build pass. The final verification re-runs `npm run verify` on an otherwise idle machine and reports the outcome as it is. — The brief puts Z38 out of scope, and the test's own code is untouched by Part 2 (Part 1 recorded the same flake). — Medium: `npm run verify` can exit 1 on a loaded machine until Z38 is addressed. |
| WP-04.2 E20 | **Ruling (final review, Minor 4, follow-up):** `insideOf`'s resolved comparison runs `realpathSync.native` synchronously whenever the textual check finds no relation. That covers `plan()` (on each keystroke in the create dialog) and `sourceNotePath()` (on each finding selection). It is not memoised or skipped in this part. — The spec required synchronous resolution for `plan` (NE15). Locally it costs microseconds, and a memo keyed by path string could go stale when a folder appears. — Medium: on a mapped or network root that goes offline mid-session, each selection can block the renderer for the SMB timeout. A follow-up should memoise per path for the session, or skip UNC roots. |
| WP-04.2 E22 | **Ruling (amends E19 with evidence; systematic debugging):** the Z38 failures are load-sensitive and not a Part 2 regression. Compared on the same machine in the same hour:<ul><li>Part 1's head (`353e2c1`) failed Z38 in 1 of 3 full-suite runs (52.5 ms).</li><li>Part 2's head failed in 7 of 10 (52–101 ms, in the `hang` or `streamed` mode).</li><li>Alone, Z38 passes on both (19–28 ms).</li></ul>No Part 2 change touches the runner, the fake fallow or the sampler (`git diff 353e2c1 -- tests/integration src/adapters/fallow` is empty). The higher rate fits a parallel pool 4 test files larger on a machine with about 40 foreign node processes and other Obsidian instances running. `npm run verify` exited 0 on the last run at `2448db1`. The gate evidence's headline keeps the fix wave's measured failing run, and this ledger records both outcomes. — Z38 is out of scope by the brief. The root cause is CPU contention against a fixed 50 ms event-loop budget, a test-budget question for the owner (Z38). — Medium: `npm run verify` stays intermittently red on a loaded machine until Z38's budget is ruled on. |
| WP-04.2 E21 | **Ruling (final review):** one fix wave covers Important 1 (the evidence headline states the measured counts and `npm run verify`'s exit code) and Important 2 (`bindFrom` waits until its button is clickable). It also takes the cheap Minors 1, 2, 3, 5 and 6: the dead `makeJunction`, the duplicated native helpers moved into `host-probes.ts`, `ROUTE_META` in `selectFinding`, the alias test's cleanup guard, and a pin of all four watched slices. Minor 4 is E20, and Minor 7 (ledger order) was done by the controller. The final review triaged the other 17 deferred minors as acceptable follow-ups. — Only two findings were Important, and each Minor was a one-to-few-line change in test files. — None. |

Also recorded during execution:
- **The full suite after Task 11** (`3275092`, the last task that touches `src`): 306/308 files passed, 3462 tests passed and 1 skipped. Two tests failed:
  - the evidence file count (`tests/unit/gate-evidence.test.ts`: unit 152 against 154), which Task 12 refreshes by design;
  - the Z38 no-freeze timing test, with a 62 ms gap under load. Run alone, it passes with an 18.9 ms gap. Z38 is out of scope, as it was in Part 1.

  No cross-task regression.
- **Network error:** the Task 2 implementer was cut off once (ENOTFOUND) and resumed from its WIP commit.

- **Final whole-branch review (opus, 353e2c1..3cb53ce): ready with fixes.**
  - It found 0 Critical, 2 Important and 7 Minor issues. The Important ones were an evidence headline stating a pass count no run measured, and `bindFrom` clicking without a clickable wait.
  - It confirmed every §5 title is present exactly once, every scenario has a RED proof on record, all three `src` fixes are correct and layered, and every commit carries the literal trailer.
  - One fix wave (`007ea19`, `2448db1`; E21) closed both Important issues and Minors 1, 2, 3, 5 and 6; the scoped re-review found all seven addressed with no new issue. Minor 4 is E20, and Minor 7 (this ledger's order) was done by the controller.
  - Declined to judge, as rulings or out of scope: E14, NPF15, NPF9, E18's visible `\@`, the Z38 flake itself, E4, NP11, E15, pop-outs, axe and mobile, CI, the suite's runtime, scenario 8's count match, and E11.
- **The final review was cut off once by a network error (ENOTFOUND) and resumed.**
- **Final verification.** `src` is unchanged since `3275092`, and the fix wave changed tests and docs only.
  - `npm run test:e2e` on Obsidian 1.13.4, at `007ea19`'s tree: "Verified 36 executed native Vitest cases, including all 36 required scenarios." It passed 36/36 in 348 s, with no WebDriver error and no worker crash.
  - `OBSIDIAN_VERSION=latest npm run test:e2e` at `3275092` resolved to app and installer 1.13.7, and also passed 36/36 through the same gate.
  - `npm run test:fallow` with `FALLOW_BIN` set: 11/11 on fallow 3.27.0 (`3275092`).
  - `npm run analyze`: 9.
  - `npm run harness-shot`: 86 captures with no page error. The five `wp04-investigate-*` captures, `wp02-settings-dark` and `s05-city-dark` were looked at.
  - `npm run verify` at `2448db1`: exit 0. That run had 308/308 files, 3469 tests passed and 1 skipped, and `assert-bundle: OK — dist/main.js is 1215 kB`. It was the last of several runs; the earlier ones failed only on Z38 (E19, E22).
  - All 21 commits on the branch end with the literal trailer "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>", checked one commit at a time.

## Deferred minors

The final whole-branch review triaged all 19. Two had to be fixed before merge and were fixed in the fix wave: #17 (`bindFrom`'s recovered "not interactable" error) and #19 (the evidence never stating exit 1). The rest are acceptable follow-ups.
- **Task 1:** `openPluginSettings` calls `handleWhere` twice (DRY); `listenerCounts` skips a non-array entry in a `_` table. Scenario 1's positive controls fail closed if the table shape changes.
- **Task 2:**
  - `reconnect()` and absolute mode had not run natively. This is now resolved: Tasks 8 and 9 exercise them, green.
  - A write the watcher hears re-renders an open profile page while you type in another field (a known cost of NE9; worth a native typing check later).
- **Task 3:**
  - "Any window" canvases are checked only in `document` (NE20).
  - Scenario 2's RED fails before the reload assertions.
  - `reviewFinding`'s disposition is untyped, and its dismissed branch is unexercised.
  - `evidence()` pairs dt/dd by index.
  - The `HAS_SETTING_ITEM` duplicate is fixed in the fix wave. The machine-id literal is ruled by E6.
- **Task 4:** `recordingFindingCount` does not simulate dropping unmatched paths; the Show more loop is never exercised natively (6 rows, fewer than a page).
- **Task 5:** `fallowBinary` also requires the path to exist, which is stricter than NE7 and fails loudly.
- **Task 6:** scenario 17's `240_000` timeout is generous.
- **Task 8:**
  - The `realPath` dependency is optional: it fails safe, and native scenario 22 guards the wiring.
  - The "only adds" tests cover `plan`, not `sourceNotePath`.
- **Task 9:**
  - Scenario 23 scans before Connect (E16).
  - The status bar covers the notes panel's buttons; the `centred()` workaround stands. This is a product layout question for a follow-up.
- **Task 10:** the same canvas, camera and selection cannot rule out an identical dispose and reconstruct (the ceiling of NE13's check).
