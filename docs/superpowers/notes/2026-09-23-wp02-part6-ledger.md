---
project: codebase-inspector
title: WP-02 Part 6 — SDD ledger (rulings)
date: 2026-09-23
branch: feat/wp-02-part6
---

# WP-02 Part 6 — rulings

This ledger records every ruling made while planning and executing Part 6, and what each one costs if it is wrong.

- Spec: `docs/superpowers/specs/2026-09-23-inspector-ui-part6-design.md` (Y1–Y40).
- Plan: `docs/superpowers/plans/2026-09-23-inspector-ui-part6.md`.
- Precedent, all binding:
  - the Part 3 ledger (R1–R9, E1–E55), cited as E<n>;
  - the Part 4 ledger (S1–S27, X0–X19, E1–E22), cited as "Part 4 E<n>";
  - the Part 5 ledger (T1–T32, E1–E25), cited as "Part 5 E<n>".
- Numbering: planning rulings here are U1…; execution rulings are "Part 6 E1"….

## Planning rulings

### Scope and owner decisions (Phase 1 design review)

| # | Ruling | Cost if wrong |
|---|---|---|
| U1 | Part 6 covers four things: the cancelling state with T29; durable review state (called WP-05 in the Part 4/5 ledgers); fallow **import only**, meaning 02.1–02.3 and 02.6 with the S15 lens; and the harness. The execution half (02.4 binding and trust, 02.5 runner, 02.7 side-effect tests, "Run fallow analysis") is **Part 7**. Owner's choice. | Medium. Part 7 inherits a finished evidence pipeline; its runner only has to produce the same raw JSON. If the owner later wants both halves shipped together, PR #1 carries the import half alone for a while. |
| U2 | Review state is stored in `data.json` under `reviews[repositoryId]` (owner's choice, Y5). It travels with the profile through Sync and backups, which architecture §6 allows for shareable profile metadata. | Medium. Review notes become visible to anyone who syncs the vault. That is consistent with profiles already living there, but it is not machine-private. A future "keep reviews local" option would need a second adapter behind the same port. |
| U3 | Imported findings are **session-only** (owner's choice, Y28). After a restart every codebase reads "Not analysed" until a report is imported again. | Low. The user re-imports after a restart. Durable evidence belongs to the Snapshot History deliverable. |
| U4 | "Not analysed" replaces every sample **finding** (owner's choice, Y33/Y34). Other sample signals stay sample and labelled. | Low. Quality, File and Overview are emptier without a report, but they no longer show invented findings. The harness `?report=demo` keeps the screenshots meaningful. |
| U5 | The S15 lens is in scope (owner's choice, Y40). | Medium. It touches the renderer port, which is a §4 contract change authorised by Y40. It is recolour-only, and tests cover a rebuilt renderer. |
| U6 | Approach A (owner's choice). An adapter layer produces raw reports, and a pure application normaliser turns them into findings. Evidence is held in a plugin-level in-memory repository shared by leaves, with a per-leaf Pinia store bound per codebase. | Low. Two leaves on one codebase agree. Part 7's runner plugs in at the same seam. |
| U7 | T29 is folded into the cancelling item: the toolbar Scan button becomes `aria-disabled` while running **or** cancelling, with a guarded handler (owner's choice, Y3). | None. It closes a carried-over deferral. |

### Design rulings from drafting and reconciliation

| # | Ruling | Cost if wrong |
|---|---|---|
| U8 | **The fallow raw schema uses `z.object` (strip), not `z.looseObject`.** The Global Constraint is amended. With zod 4.6.5, `z.looseObject` keeps undeclared keys in the parse output, so `fragment` (source text), `actions` and `_meta` would survive into memory, breaking "source content is never stored" (Y23). `z.object` tolerates additive fields exactly as well, and drops them. | Low. A future fallow field we want must be declared, which is the intended discipline anyway. |
| U9 | **All fallow code lives in `src/application/evidence/`**: `raw-fallow.ts`, `fallow-report-schema.ts`, `read-fallow-report.ts`, `model.ts`, `normalize-fallow.ts` and `resolve-findings.ts`. There is no `src/adapters/fallow/`. Nothing in `src/application` imports `src/adapters`. The UI's dialog therefore reaches the reader through the existing ui → application edge, and no ui → adapters edge is created. The reader uses only zod and `Blob.text()`, with no Node API. | Low. Part 7's runner is a genuine adapter (process I/O) and will live in `src/adapters/`, producing the same `RawFallowReport` type. |
| U10 | `NormalizedEvidence.notShown` is `{ key, count }`. The UI labels it with `fallowNotShownLabel(key)`, which uses own keys only and falls back to the raw key. It never returns an `Object.prototype` member. Application code holds no UI copy. | None. |
| U11 | Finding ids are `CX-`/`DU-`/`UN-` followed by `fnv1a32Hex` of a stable key (Y24), from a new `src/domain/hash.ts`. The three existing private fnv copies are left alone. **Duplication ids change on a fallow upgrade**, because clone fingerprints differ between 3.21.0 and 3.27.0, so a disposition on a duplication finding does not re-attach across an upgrade. Complexity and unused-export ids are stable. | Low. A reviewer re-decides duplication findings after upgrading fallow. Keying on instance ranges instead would break on any edit. |
| U12 | The normaliser is **snapshot-independent**. `resolveFindings` matches paths against a snapshot separately, so the evidence index re-resolves the same report against a newer snapshot (stale, Y30). | None. |
| U13 | `source-mismatch` is decided in the UI after resolving, with the offered mapping tried first. The COPY-17 wording is adapted, because a fallow report has no source revision: "…none of its findings names a file in the current snapshot, even with the offered folder mapping. Nothing was imported." | Low. |
| U14 | The fixture project has **five** `.ts` files plus `package.json` (the spec is corrected). It is excluded from `tsconfig.test.json`, eslint and oxlint. The controller records the raw outputs **before** Task 5 is dispatched, in Git Bash (PowerShell 5.1's `>` writes UTF-16), from the npx-cached 3.27.0 and 3.21.0 binaries, with no download. Values hard-coded from the dry run are checked by a node script before the commit. | Low. If a recording differs from the dry run, the check fails before anything is committed, and the plan's asserted values are corrected in the ledger. |
| U15 | The review port has **13 members**: the 9 existing ones plus `allocateId`, `subscribe`, `diagnostics` and **`replaceAll(state)`**. Store `replaceAll`/`clearAll` call it **once**, so a bulk operation is one atomic `data.json` write and one notification. Without it, 2000 items meant 2000 full rewrites and 2000 reloads in every other leaf. Part 5's `allSettled` per-record loops and their partial-failure tests are replaced by atomic behaviour. Part 5 E18–E20 still hold: `bulkBusy`, the refusals, and staying bound to the starting codebase. | Medium. A failed import now changes nothing, where before it could leave a partial state. That is strictly better for the user, but the Part 5 tests that pinned partial state are rewritten, so a reviewer must check the rewrite keeps E18–E20. |
| U16 | A durable `replaceAll`/`clearAll` replaces the **whole** record set, including records that were skipped but kept. It is an explicit user replace or clear. Ordinary upserts and removes never touch skipped records (Y7). | Low. A user who clears review state also loses unreadable hand-edited records, which is what Clear means. |
| U17 | The durable adapter's `allocateId` **throws** before the adapter has absorbed the stored set. Every write also re-absorbs `highWater` and every raw id under the lock. The store's `ready` gate (adds refuse with `null` until the first load has finished) makes the throw unreachable from the UI. The in-memory adapter never throws. | Low. A future caller that allocates without loading gets a developer error, not a silently overwritten record. |
| U18 | Every durable `list*` re-reads `data.json`, sharing a read already in flight. This resolves Y8's "cache after first read" against Y19's "picked up on the next bind" in favour of Y19. Only the id marks and the diagnostics are cached. | Low. One `loadData` per `load()`. |
| U19 | `review-record-codec.ts` sits in `src/ui/read-models/` and reuses the Part 5 export and import helpers. The durable adapter imports it and the port from `src/ui/`. These are the first adapters → ui edges. No lint rule forbids them, and the port has lived in `src/ui/stores/ports` since Part 4. | Low. Moving the port and codec to the application layer later is a mechanical rename. |
| U20 | `encodeWorkItem`/`encodeDisposition` take `repositoryId` and refuse a file target or fingerprint that belongs to another repository, or that is not a file entity. | None. It prevents a silent re-homing of paths. |
| U21 | A purge retires the adapter instance (`retire()`): later writes reject `'unsupported'`, and its subscribers are notified once. A leaf still showing a removed profile therefore cannot recreate `reviews[id]`. `registry.for('')` returns an unpersisted in-memory repository. | Low. |
| U22 | `detach()` (review store) and `unwireDataPorts(pinia)` (`src/host/data-ports.ts`) are added. `CityView.onClose` calls `unwireDataPorts` before dropping the pinia. Nothing in the codebase ever calls `$dispose`, so without this a closed leaf's store would stay subscribed to the plugin-level repositories. | Low. Covered by a host test. |
| U23 | The evidence store and the lens store are Pinia **setup** stores; every other store is an options store. The evidence store uses setup so that `onScopeDispose` runs on `$dispose`. The lens store uses setup so that it can watch the evidence store with `flush: 'sync'`, which is load-bearing: the harness sets the lens right after `attach()`. | Low. |
| U24 | Store `loadFailed` is set when a load rejects, and Settings shows `REVIEW_STORE_READ_FAILED`. App's `void review.bindRepository(id)` gets `.catch(noop)`, so a durable read failure is never an unhandled rejection. A failed background reload (another leaf's notification) is silent under E17, except for `loadFailed`. | Low. |
| U25 | Settings › Clear review state becomes `aria-disabled` with the Import hint while no snapshot is on screen. Otherwise Y14's refusal of the unbound bucket would show the wrong "a change is pending" message. | None. |
| U26 | Task 4 rewrites every review-state string that says "session": `SETTINGS_STORAGE`/`_TEXT`, `STORAGE_DISCLOSURE_TEXT`, `SETTINGS_CLEAR_TEXT`, `SETTINGS_IMPORT_TEXT`, `FINDING_DISMISS_HINT`, `FINDING_DISMISSED`, and the export's `REVIEW_STATE_NOTE` (the text changes, the format does not). | Low. Wrong copy would misstate where data lives. That is why the task gives exact strings. |
| U27 | `ReviewStoreError` codes (`full`, `unsupported`, `unrepresentable`) stay unmapped by the screens, which keep announcing their generic `*_FAILED` strings (spec §4). **Deferred.** | Low. A user whose codebase hits 1 MB sees "could not save" rather than "storage full". |
| U28 | `hasPendingChanges` becomes per codebase (Y15). A save in flight in codebase A no longer blocks Clear or Import in codebase B. `bulkBusy` stays per store. This closes Part 5 E15. | Low. |
| U29 | The Y16 test (`bulkBusy` clears when `replaceAll`'s own reload rejects) is a regression pin: it passes before Task 2. This is an exception to "every new test fails RED", recorded here. | None. |
| U30 | There is one cancelling copy function, `CANCELLING_BANNER(hasSnapshot)`, used by both the banner and the announcement (Y1/Y2). The skeleton's constant `CANCELLING_ANNOUNCEMENT` is dropped. | None. |
| U31 | There is **one shared evidence test fixture**, `tests/fixtures/evidence-report.ts`. Task 7 creates it and Task 8 completes it. `syntheticFallowJson(snapshot, options)` returns real-shaped fallow 3.27.0 JSON, and every synthetic report goes through the real `parseFallowReportText` → `buildEvidenceReport`. `tests/fixtures/data-port-deps.ts` / `dataPortDeps()` (Task 4, widened by Task 7) is the only `CityViewDeps` test helper. | Low. Three drafters had invented three fixtures. |
| U32 | Task 8 owns `findings.ts`: `severityRank`, `severityTone` (critical/high/moderate/unrated), `findingsCsv(rows, evidence)`, `QualityModel.evidence`, and the copy moves (`SEVERITY_LABEL`, `FINDING_SEVERITY_UNRATED`, `SEVERITY_TEXT`, `FINDING_TITLE` → `audit-copy/quality.ts`). The kit severity classes become `--critical`, `--high`, `--moderate` and `--unrated`; `--medium` and `--low` are retired. Only the findings screens used them. Task 8 fixes every existing test that breaks when the samples go. Task 9 builds on Task 8's final text and does not duplicate these helpers. | Medium. The Task 8/9 seam is the plan's densest. The pre-flight scan re-checks it against the real code after Task 8. |
| U33 | The findings total is `partial` when a report analysed only some categories (for example `dead-code` only), with reason `FALLOW_SOME_NOT_ANALYSED`. `partial` is an existing `EvidenceState`. | Low. |
| U34 | Task 8 removes the three per-row "Sample" provenance badges, and Task 9 adds the EvidenceBadge where Y35/Y36 want it. | None. |
| U35 | `use-read-models` shows a report only when the evidence store's `repositoryId` equals the snapshot's. This prevents one frame of another codebase's evidence during a switch (Part 4 E8/E11). | None. |
| U36 | `COPY_16(absoluteDate)` (stale evidence) is defined by Task 8 in `audit-copy/fallow.ts` and imported by Tasks 9 and 11. `REPORT_EVIDENCE_TEXT` is rewritten by Task 9. Task 8 rewrites the Overview evidence-coverage row `static` as `fallow`: collected, stale or unknown, never sample. | None. |
| U37 | `EvidenceBadge` props are `{ version, state }`. The provider and "unverified source match" are fixed in copy, and the spec is amended. | None. Part 7 can add props. |
| U38 | The renderer member is `CityRendererPort.setReported`, the real interface name (the outline said `RendererPort`). The set is kept inside the renderer, so it survives `setColors`, a set sent before the first layout, and a rebuilt city. `useLensRenderer` re-sends it to every new handle. `CityViewport.vue` (400/400) is untouched. | Medium. A lens that silently drops after a context loss would mislead. Tests cover rebuild, pre-layout and `setColors` order. |
| U39 | The 18 existing renderer test doubles each get `setReported: vi.fn()` **on an existing line**. `city-view-store-wiring.test.ts` is touched but not grown (450/450), which satisfies "never grow it". | None. |
| U40 | The findings-lens "Reported" column shows in **list mode only**. On the canvas, the city carries the lens. | Low. A one-condition change if the owner wants it in the wide layout. |
| U41 | The new shots use the WP-02 naming: `wp02-city-cancelling-dark`, `wp02-sources-fallow-dark`, `wp02-connect-fallow-review-dark`, `wp02-quality-fallow-dark`, `wp02-city-lens-dark`, plus `wp02-city-lens-light`, because S15's acceptance asks for both themes. The harness report data is synthetic, and a page footer says so. | None. |
| U42 | The no-process guard (`tests/unit/no-process-execution.test.ts`) bans:<br>• the `child_process`/`worker_threads` names;<br>• `spawn`/`exec`/`execFile`/`fork` calls, including their sync forms, as bare calls or members;<br>• `openPath`.<br>It exempts a member `.exec(` (`RegExp.prototype.exec`, used twice today). Part 7 must amend it deliberately. | Low. |
| U43 | `clean-vault-install.test.ts` checks the command ids against the `checkpoint4:commands` block in `2026-09-17-wp01-implementation-report.md`, so Task 7 adds the new command there. | None. |
| U44 | Requested opens of the S14 dialog wait one tick. The route-focus post-watcher (Part 5 V7) then lands first, and `CiDialog` records the page title as its opener. The dialog's hidden file input sits outside the `CiDialog` panel, so it never joins the focus trap. | Low. |
| U45 | `Import report` on Quality and File detail navigates to Data & scans and requests the dialog, like the command does. The file picker always opens from a real click inside the dialog. | None. |
| U46 | External changes to `data.json` (Sync, hand edits) are picked up on the next bind (Y19). An id allocated in between could collide with an externally added record. **Deferred.** | Low. It needs a concurrent external edit that adds a higher id during a session. |
| U47 | The pre-existing missing catch around `profileStore.remove` in `settings-tab.ts` `deleteProfile` is not fixed here. **Deferred.** | Low. A failed profile removal surfaces as an unhandled rejection, exactly as before Part 6. |
| U48 | `QualityModel.severities` (the severities present, in `severityRank` order) feeds the Quality severity filter. An unknown future severity word stays filterable. | None. |
| U49 | `FallowReportFacts.vue` is a third sources component. The fallow card and the S14 review step both show the same `<dl>`, rendered once, and the XSS test covers both. | None. |
| U50 | The shared fixture gains `snapshotWithOnlyFiles` (Task 11), because the synthetic report puts a finding on every file of its snapshot. The harness `?report=demo` covers ten harness files: 19 findings on 10 files, plus one unmatched path. | None. |
| U51 | Task 4's copy table rewrites 14 strings: the seven in U26, plus `IMPORT_REPLACE_TEXT`, `WORKBENCH_CARD_TOTAL_CAPTION`, `WORK_DELETE_CONFIRM_TEXT` and `PLAN_MD_NOTE`, which also said "session", and the new `REVIEW_STORE_READ_FAILED`, `SETTINGS_CLEAR_HINT` and storage-line strings. The Clear hint has its own wording, because the Import hint talks about file paths. | Low. |

## Execution rulings

### Pre-flight conflict scan (before Task 1, HEAD 98d19ae)

Five read-only scanners checked every task's text against the real code at HEAD, against the spec, and against the text of the tasks it consumes. One row per task and one row per task pair that shares a file or an interface.

| Scope | What was checked | Finding | Ruling |
|---|---|---|---|
| Task 1 | Every anchor in view-surface, AppToolbar, AnnouncementRegion, inspector-copy, harness seed/mount/page, and the four tests it edits; RED claims | OK. A blocked Scan still takes the kit hover fill (kit.css:267), as Cancel already does | E21 |
| Task 2 | Port, buckets and store text; review-store 400 → 323; the four hand-built 9-member port doubles; the A1 test rewrites | OK, complete. The Y16 test is really RED at HEAD; `noDiagnostics` is defined twice; the spec's Y13 wording differs | E6, E10, E11 |
| Task 3 | Storage helpers, record schemas, `findingRef`/`exportedTarget`, the contract cases traced by hand | OK. The durable `removeDisposition` with an unstorable key does not notify; the storage `export *` lands under Task 1's comment; `listOf` repeats `asUnknownArray` | E8, E9, E21 |
| Task 4 | 14 `CityViewDeps` literal sites in 8 files; the 14 copy strings; the settings-tab constructions; the city-view budget (275 → 278) | OK. Its two new host tests do not spread `dataPortDeps()` | E3 |
| Task 5 | Step 0 anchors; zod 4.6.5 strip behaviour; the no-process guard over `src` (0 hits) | OK. Step 0's value check misses several values the tests hard-code | E7 |
| Task 6 | `normalizeRelativePath`, `FINDING_ID_PATTERN`, `hash.ts`; ids, order and prefix cases traced by hand | OK. `suggestStripPrefix` adds two conditions that are in neither Y26 nor a ruling | E12 |
| Task 7 | commands, the report `checkpoint4:commands` block, App anchor, city-view 278 → 284, Pinia setup-store `$dispose` | `[...set]` fails oxlint `unicorn/no-useless-spread` (also in Tasks 2 and 3) | E5 |
| Task 8 | Every consumer of the sample findings and of the retired severity classes; every anchor; the fixture arithmetic | **The "Post-Task-8 file state" appendix is missing from the plan**, and Step 8(b) replaces the whole of `findings.ts` with it | E1 |
| Task 9 | Every anchor against Task 8's text; line caps; css-class-scope | The Illustrative sweep can never be empty; the Export button keeps native `disabled`; a report with no findings on screen reads "No findings match these filters" | E13, E14, E15 |
| Task 10 | Every anchor at HEAD; the tests against Tasks 5–9's code | OK. `fallow-candidate.ts` is a screen-folder module that imports `src/application` | E20 |
| Task 11 | The renderer doubles (18 at HEAD, each on an existing line); component anchors | OK. It pluralises again instead of reusing `nounCount`; the toolbar gates on the raw store | E17, E18 |
| Task 12 | Harness anchors; the count procedure against fb84ca3/d5ee2c9 | OK. The demo files miss `dir-2`; two Step 8 visual checks are worded so that they cannot hold | E19 |
| 1 ↔ 11 | Both edit AppToolbar.vue | OK. The two tasks edit disjoint lines | — |
| 1 ↔ 12 | `?run=cancelling` vs `?report=demo` in the harness | OK. Task 12's anchors avoid Task 1's lines | — |
| 2 ↔ 3 | 13-member port, `ReviewReplaceState`, the id helpers, the notification contract | OK, except the unstorable-key removal | E8 |
| 2 ↔ 4 | `setRepositoryFactory`, `detach`, `ready`, `loadFailed`, `storageDiagnostics` | OK. Settings › Clear shows the wrong reason between Tasks 2 and 4 | E16 |
| 3 ↔ 4 | Registry, purge, storage copy | OK | — |
| 3 ↔ 1 | inspector-copy `export *` lines | The comment block is mislabelled | E9 |
| 4 ↔ 7 | `data-ports.ts`, `dataPortDeps()`, App's `.catch(noop)` line | **Blocker**: Task 4's `city-view-data-ports.test.ts` literal and `data-ports.test.ts` cast break Task 7's typecheck and runtime | E3 |
| 4 ↔ 11 | Task 4's `inertPort: CityRendererPort` is a 19th double | Task 11 misses it | E22 |
| 4 ↔ 8, 4 ↔ 9 | `FINDING_DISMISS_HINT`/`FINDING_DISMISSED`, `QUALITY_FOOTNOTE` | OK. `QUALITY_FOOTNOTE` says "session" between Tasks 4 and 9 | E16 |
| 5 ↔ 6 | fixture helper and raw types | OK | — |
| 5 ↔ 7 | `audit-copy/fallow.ts` appends | OK | — |
| 5 → 10 | reader, error copy, `fallowNotShownLabel` | OK | — |
| 6 ↔ 7, 6 ↔ 8, 6 → 10 | `EvidenceReport`, `resolveFindings`, `buildEvidenceReport` | OK | — |
| 7 ↔ 8, 7 ↔ 9, 7 → 10 | evidence store API, the shared fixture | OK | — |
| 7 → 10 | `city-view-evidence.test.ts` reads `importRequested` after a flush | It breaks once SourcesScreen consumes the request (Task 10) | E4 |
| 8 ↔ 9 | Task 9's Edits against Task 8's text | 3 of the 5 `findings.ts` anchors exist only in the missing appendix; Task 9 breaks Task 8's `file-detail-model` title assertion | E1, E2 |
| 8 → 10, 9 → 10 | index, `COPY_16`, EvidenceBadge | OK (`FINDING_KIND_LABEL` already exists at HEAD) | E21 |
| 8 ↔ 11, 9 ↔ 11 | index, fixture, EvidenceBadge | OK | — |
| 10 ↔ 11 | `audit-copy/fallow.ts` | Duplicated pluralisation | E17 |
| 5–11 → 12 | harness seed, captures, counts | OK apart from the Task 12 row | E19 |

| # | Ruling | Cost if wrong |
|---|---|---|
| Part 6 E1 | **Task 8's "Post-Task-8 file state" appendix was dropped when the plan was assembled.** It is recovered verbatim from the Phase 1 drafting file (`tasks-07-08.md`, whose Task 7–8 text is byte-identical to the plan's) and goes into the Task 8 and Task 9 briefs. It meets every requirement the scan derived from Task 8's tests and Task 9's anchors: a Map-backed `severityRank`, `filterFindings` searching the symbol, the base memo keyed per `EvidenceIndex` (E53), priority-then-report order, and the literal lines Task 9 anchors on. Its `quality.ts` shows the pre-Task-4 dismissal strings, and A6 already rules on that. Before Task 9, the controller re-reads Task 8's committed files and re-anchors (A5). | Low. If a detail of the appendix disagrees with a Task 8 test, the test wins, and the implementer reports the difference. |
| Part 6 E2 | Task 9 updates Task 8's `tests/unit/file-detail-model.test.ts` title assertion to `FINDING_TITLE_FOR(...)`, and adds the file to its lists. | None. |
| Part 6 E3 | Task 4's `tests/host/city-view-data-ports.test.ts` spreads `...dataPortDeps()` into its `CityViewDeps` literal, and `tests/host/data-ports.test.ts` builds deps as `({ ...dataPortDeps(), ...ports }) as CityViewDeps`. That follows A2/U31 (one deps helper), and Task 7's claim that it edits no deps literal then holds. | Low. Without it, Task 7 fails typecheck and every data-ports test throws. |
| Part 6 E4 | Task 7's `city-view-evidence.test.ts` asserts `importRequested === true` synchronously, straight after `openReportImport()`. It then flushes and asserts the route. Task 10's SourcesScreen consumes the request on mount, so an assertion after the flush would break at Task 10. | None. |
| Part 6 E5 | Copy-before-iterate over a Set is written `Array.from(set)`, not `[...set]` (oxlint `unicorn/no-useless-spread` is an error here). This applies to Tasks 2, 3 and 7. | None. |
| Part 6 E6 | The Y16 test is **really RED** at HEAD, on its last line (`expected 'wi-1' to be 'wi-13'`), because the rejected reload never advances the old counter. The A9/U29 exemption is moot: the implementer records the real RED and must not weaken the test. | None. |
| Part 6 E7 | Task 5 Step 0 is committed as `test(fixtures): fallow raw fixtures (controller)`. The owner's instruction overrides the plan's message. The node value check also prints health `col`, `is_type_only`, the `check.summary` keys and values, whether `actions`/`fragment` are present, and 3.21.0's severity/`exceeded` types. It stops on any value the Task 5/6 tests hard-code that differs (U14). | Low. |
| Part 6 E8 | Task 3's durable `removeDisposition` notifies once even when the fingerprint cannot be stored, so the port contract "every successful write call notifies once" holds. Otherwise the store's `ownWrites` slot would swallow the next foreign notification. | None. |
| Part 6 E9 | Task 3 puts its own `/** Part 6: durable review state. */` comment above `export * from './audit-copy/storage';`, so Task 1's cancelling comment labels only its own line. | None. |
| Part 6 E10 | Task 2 exports one `noStorageDiagnostics()` from the port file, and the store imports it, so the `{ skipped: 0, unsupported: false }` factory is defined once. | None. |
| Part 6 E11 | Spec Y13's "≤ 320 lines" target and "buckets hold bindRepository's steps", and Y12's "store-held ownWrites", are satisfied in substance: the store is 323 lines under the 360 budget test, bind stays in the store over bucket helpers, and `ownWrites` lives per bucket, which is strictly more precise. The 360 budget test governs. | Low. A later reader expecting 320 finds 323. |
| Part 6 E12 | Task 6's `suggestStripPrefix` is narrower than Y26: no snapshot path may already lie under the prefix, and a tie between two shortest candidates offers nothing. Accepted, because an ambiguous mapping offers nothing rather than guessing. The task's "Y23" cite reads Y26. | Low. A report with two equally short candidate prefixes gets no offer and must be re-rooted. |
| Part 6 E13 | Task 9's Quality **Export** button uses `aria-disabled` plus a guarded handler, not native `disabled`. Its rows can now empty while it has focus, when another leaf removes or replaces the shared report (E40/E44/E50). The test asserts `aria-disabled`. | Low. |
| Part 6 E14 | Quality with a report attached and zero findings on screen shows a "no findings reported" note (`QUALITY_NO_FINDINGS_REPORTED`, "No findings reported for this codebase. That is not the same as zero complexity.") in place of "No findings match these filters" and its Reset. A clean fallow run is a plausible report. File detail already distinguishes this case (Y36). | Low. One more string and one branch. |
| Part 6 E15 | Task 9's `Illustrative` sweep is scoped to `src/ui/screens/quality`, `src/ui/screens/file` and `src/ui/audit-copy/quality.ts`. The four unrelated hits (dependencies, report, and two in inspector-copy) are other sample copy and stay. | None. |
| Part 6 E16 | Interim states between commits are accepted, because the branch lands whole. Settings › Clear shows the busy reason with no snapshot (Tasks 2→4). `QUALITY_FOOTNOTE` says "session" (Tasks 4→9). The storage copy mentions imported findings before import exists (Tasks 4→10). Real findings read "Sample finding" (Tasks 8→9). | None for users. A mid-branch checkout shows stale copy. |
| Part 6 E17 | Task 11's `LENS_SUBTITLE` and `LENS_LIST_CELL` reuse Task 10's `nounCount`, so counts format the same way on the lens and the card. | None. |
| Part 6 E18 | Task 11's toolbar lens `<select>` renders on `useLensView`'s evidence state (≠ `'none'`), the one derivation the legend, heading, list column and renderer use. It no longer renders on the raw store's report. `setLens` keeps its own store-level guard. | Low. |
| Part 6 E19 | Task 12's `DEMO_FILE_INDEXES` replaces 88 with 86, so every district has a reported file; the counts are unchanged. Step 8's visual checks are reworded as follows. The lens framing may differ by the heading's height, but there is no relayout. The no-report fallow card reads Unknown with "No report attached…". The synthetic footer must hide nothing the checks look at. | None. |
| Part 6 E20 | Task 10's `fallow-candidate.ts` lives in `src/ui/read-models/`, not in the screen folder, because screens read read models, stores and copy only. The import paths follow. | Low. A mechanical move. |
| Part 6 E21 | These minors are accepted without change:<br>• A blocked Scan still gets the hover fill; this is the Cancel precedent.<br>• RED-message wording nits in Tasks 2, 5 and 8.<br>• plan-head's "twelve members" means thirteen.<br>• The codec's `listOf` repeats `asUnknownArray`. Importing it would add a ui → adapters edge.<br>• Task 7's `counting()` test helper appears twice.<br>• Task 9's two-line `importReport()`/badge-state glue.<br>• Task 10's mislabel of `FINDING_KIND_LABEL` as a Task 9 product.<br>• Task 9's mid-line import Edit. The brief says to match it as a substring. | Low. The final review triages them again. |
| Part 6 E22 | Task 11 adds `setReported: vi.fn()` to every typed `CityRendererPort` double: the 18 at HEAD, plus any that Tasks 4 and 7 add (at least `inertPort` in `city-view-data-ports.test.ts`). Typecheck finds them. A10's "18" means 18 at HEAD. | None. |

### Per task

| # | Task | Ruling | Cost if wrong |
|---|---|---|---|
| Part 6 E23 | 1 | Task 1 is complete, and both reviews passed at the first round (1efc600..8ed0abe). Two minors are deferred to the final review: the guarded-press test does not assert "no announcement", because nothing wires Scan to the region; and `cancelScan`/`requestScan` are two near-identical two-line guards, which is the pre-existing idiom. | None. |
| Part 6 E24 | 2 | The quality reviewer's Minor 1 is **promoted and fixed inside Task 2**. The Y12 background reloads on the *same* repository let a reload that started before an own write finish after it, erasing the write from the leaf's view and letting a duplicate (target, intent) through. A stale failing load could also set `loadFailed` after a newer success. Task 3's durable `list*` does real I/O, which makes the race reachable from the UI. The fix: a per-bucket load ticket, so only the latest load applies; and an own write that settles while a load is in flight starts one more reload. Both are pinned by RED-first tests (3ae26ca). | Low. One extra `list` (one data.json read) when a write settles during a load. |
| Part 6 E25 | 2 | The implementer's `ownWrite` is **not** `async` (a deviation from the task text). It returns the port's own promise and gives the slot back in a side `.catch`, attached before the caller's `await`. That keeps "a rejection gives its slot back before the caller resumes", adds no extra microtask turn, and leaves no unhandled rejection; both reviewers traced it. `vi.fn(inner.allocateId)` becomes an arrow wrapper for eslint `unbound-method`. Task 2 is complete after 1 fix round (21c0e67..3ae26ca). Deferred minors: `replaceAll`/`removeRule` read `boundKey` where the other actions use a captured key; `load()` looks the bucket up again; a reload error hides the port's error when both reject; `ownership-screen`/`quality-screen` tests count microtask turns instead of using `flushPromises` (Part 4 X9); no test pins the stale-*success* drop on its own; a rejected own write during a load reloads once more. | Low. A future change that makes the port's notification asynchronous breaks the counting; Task 3's contract pins "notifies exactly once, before resolving". |
| Part 6 E26 | 3 | Both reviews approved at the first round. Four of the quality reviewer's minors are **promoted** and fixed in Task 3 (b854c9a):<br>• **The real bug.** One hand-edited record id or `highWater` above 999999 made every later id unrepresentable, so every add in that codebase was refused forever. Raw ids and marks above `STORED_ID_SUFFIX_MAX = 999_999`, the schemas' 6-digit limit, are now ignored.<br>• Three missing pins:<br>&nbsp;&nbsp;– `retire()`'s stale-read guard (the E24 race);<br>&nbsp;&nbsp;– a failed `saveData` notifies nobody and changes nothing;<br>&nbsp;&nbsp;– ordinary writes keep unknown set and `highWater` keys.<br>Each one was RED when its line was reverted. | Low. An id above 999999 written by a hand edit is ignored for allocation, so it could collide only with a record the schema already refuses to read. |
| Part 6 E27 | 3 | Task 3 is complete after 1 fix round (1d69b0f..b854c9a). Deferred minors, for the final review:<br>• the 1 MB save test seeds a set that is already over the limit rather than one the save crosses;<br>• a refused write still raises the in-memory marks (gaps only);<br>• no round-trip case for paths with spaces or unicode, and none for a `__proto__` repository id;<br>• `isPlainObject` is duplicated in the adapter and the codec;<br>• `write(…, true, true)` takes positional booleans;<br>• a no-op removal, or a purge on a fresh install, saves data.json;<br>• a retired instance's diagnostics say `unsupported: false`;<br>• `registry.for('')` returns a new in-memory repository per call (plan-mandated, U21);<br>• no test pins the shared in-flight read;<br>• no case pins an ordinary write keeping `set.extra` (now covered by E26's case). | Low. |
| Part 6 E28 | 5 (pre-dispatch) | Task 5's `export * from './audit-copy/fallow';` goes after Task 3's storage line, under its own `/** Part 6: fallow report import. */` comment, not straight after the city line as Step 9 wrote it. The comment blocks then label their own lines. No later task anchors on either line. | None. |
| Part 6 E29 | 4 | Both reviews passed at the first round. Three quality minors are **promoted** and fixed in Task 4 (cafdc5c):<br>• **Data loss.** While the bound read had failed or not finished, Settings › Clear/Import stayed enabled, the dialog counted the empty in-memory lists ("removes 0…"), and a transient failure could let the atomic `replaceAll` wipe a saved set. Now the store's `replaceAll`/`clearAll` refuse with `false` while `!ready \|\| loadFailed`, and both rows are `aria-disabled` through one shared `review-write-gate.ts`, the hint pointing at the `REVIEW_STORE_READ_FAILED` line.<br>• **Untruthful notice.** A failed purge showed only the raw error. It now says the profile was removed but its review decisions could not be deleted (`PROFILE_REVIEW_PURGE_FAILED`).<br>• **Missing pin.** A rejected profile removal never purges. | Low. While the first read is in flight, Clear/Import are blocked with no described reason: a "still reading" hint would need new copy, and the window is short. |
| Part 6 E30 | 4 | Task 4 is complete after 1 fix round (0539f32..cafdc5c). The Clear gate keys on `city.snapshot` only, but the App watcher binds the review store synchronously to the snapshot's codebase before render, so they cannot disagree outside tests. Deferred minors:<br>• a Clear/Import dialog left open while a background reload fails shows the "pending change" busy text (the refusal is safe; the reason is inexact);<br>• App's `.catch(noop)` also swallows a factory/subscribe throw without `loadFailed`;<br>• two copy assertions compare a constant with itself;<br>• wiring-before-mount and main.ts's `reviewRepositoryFor` mapping are only type-checked;<br>• the `storageNote` precedence with several conditions at once is untested. | Low. |
| Part 6 E31 | 5 | The spec review passed. The quality review found two **plan-mandated** Important defects, and both are fixed rather than accepted:<br>• **Memory exhaustion (acceptance 4 / Y31).** zod 4 collects every issue before it returns, so a valid-size report (`"findings":[{},{},…]`, or a `summary` of bad values) built millions of issues: 1.2 MB → 1.27 GB of heap, which would take Obsidian down. The fix makes arrays and the summary record `unknown` in a shell schema, then walks the elements, stopping at the first failure (`firstArrayFailure`/`firstRecordFailure`). A read-counting test proves that no element is read after the failure. `FALLOW_REPORT` and `RawFallowReport` are unchanged, and so are the 13 `invalid <path>` expectations.<br>• **The no-process guard could miss real code (acceptance 2).** Its string-unaware comment stripping, then a bare token scanner (round 1), missed an appended `spawn(` in 68 of 262 `src` files. It now parses with `ts.createSourceFile`, and splits SFCs with `@vue/compiler-sfc` (added as a devDependency at the installed 3.5.43; the tests use it only). A self-test injects `spawn('x')` into every `src` file and fails on any miss.<br>• **Promoted minors:** optional, bracket, `.call`/`.apply` and unwrapped callees; any string literal naming a banned module; the Electron openers (`openExternal`, `openItem`, `showItemInFolder`), which extends U42; a source-text value scan of the parsed raw report; and the "16 MB" copy derived from the constant.<br>It took 3 fix rounds (0f71f62..2d40ac0). | Low. The guard is still "not exhaustive": aliasing (`const s = spawn`), `spawn.bind(null)()`, `Reflect.apply` and uncalled opener references are listed as accepted gaps. |
| Part 6 E32 | 5 | Task 5 is complete (080fd89..2d40ac0). A quality reviewer broke the read-only rule once: it created and deleted a probe file in the worktree, and `git status` was clean afterwards. Later reviewers probe only under the scratchpad. Deferred minors:<br>• SFC parse errors and custom blocks are skipped silently (`<SCRIPT>`, two `<script setup>`, `<docs>`); failing closed on `errors.length > 0` is the cheap fix;<br>• one HTML-comment row does not discriminate round 2 from round 3;<br>• the header's gap list misses `bind`/`Reflect.apply`/`.call` on a bracket opener;<br>• the file walker is duplicated three times in tests;<br>• `supportedText` breaks with a single kind;<br>• there is no type-level `z.infer` equality with `RawFallowReport`;<br>• edge cases are untested: a `1e400` schema, a file exactly at the maximum size, a surrogate pair split by the clip, an empty kind;<br>• the structural tests prove the helpers, not that the reader is wired to them;<br>• error precedence now names a shell failure before an element failure. | Low. |
